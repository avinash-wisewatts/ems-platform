fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary,pool]==3.2.4
jinja2==3.1.5
python-multipart==0.0.20
httpx==0.28.1
pydantic-settings==2.7.1
itsdangerous==2.2.0
argon2-cffi==23.1.0

paho-mqtt==2.1.0

# WebSocket client for main.py's Asset View live WebSocket proxy
# (proxy_asset_live_websocket -> live-telemetry's live_asset_websocket).
# Already an incidental transitive dependency of uvicorn[standard] (its
# server-side WebSocket support); pinned explicitly here because
# application code now imports it directly, not just uvicorn internally.
websockets==17.1
