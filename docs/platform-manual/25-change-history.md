# Change History

Status: HISTORICAL
Last verified: 2026-08-27
Verification basis: Audit evidence, Repository, Staging (multiple sessions)

This is a narrative timeline synthesized from the `Audit/` folder's ~35
investigation documents plus this session's own verified work. It is a
**history of investigation and implementation phases**, not a live status
document — for current state, see the numbered architecture documents
(especially [23-known-issues-and-drift.md](23-known-issues-and-drift.md)).
Audit document titles are quoted for traceability; read the source
document for full detail on any phase.

## Phase 0 — Current-state baseline (multiple audit documents)

A multi-part investigation (`Phase 0 Current-State Inventory`,
`Phase 0 Complete Grafana Analytics Consumption Audit`,
`Phase 0 Metric Lineage Catalogue`, `Phase 0 Semantic Layer Audit`,
`Phase 0 Static Analytical Query-Performance Audit`,
`Phase 0 Tenant-Isolation & Authorization Audit`,
`Phase 0 Application-API Endpoint Audit`,
`Phase 0 Runtime Verification Against STAGING`) culminating in
`Phase 0 Final Deliverable: Current-State Analytics Architecture Baseline`.
This established the layered model (raw → normalized → domain measurements
→ two parallel aggregation families → analytics/semantic layer) that
[05-database.md](05-database.md) and [11-aggregation.md](11-aggregation.md)
describe. **At this baseline, staging had zero commissioned
sites/gateways/devices** — every telemetry table below the metadata layer
was empty by construction, not by failure. The baseline also recorded live
job-schedule drift (`run_normalization_job` running 5min/20min-overlap
against a canonical 1min/15min-overlap definition) and a retention-policy
discrepancy (a "do NOT add a retention policy yet" comment in source vs. an
already-configured 180-day/2-year retention observed live).

## Phase 1 — Canonical semantic model

`Phase 1 Canonical Semantic Model` and its `Final Reconciliation` document
define the target semantic model the subsequent 1A-1E phases implemented
toward.

## Phases 1A–1E — Incremental implementation

A sequence of implementation plans and reports (1A connectivity/semantic
consolidation, 1B, 1C, 1D, 1E-A, 1E-B), each pushed to `origin/staging` as
scoped, reviewed commits. Notable: `Phase 1C Scope Ambiguity (Blocking)`
and `Phase 1D Investigation (STOP — Discrepancy Found)` record cases where
implementation paused pending clarification rather than proceeding on an
assumption — consistent with this manual's own "do not infer architecture"
posture. `Phase 1D CI Failure and Corrective Commit` records a CI break and
its fix. `Phase 1E-B Implementation Report` (historical backfill of
per-flow energy-consumption quality columns) is the most recent
implementation-phase document found in `Audit/` as of this verification;
its commit (`b114c10`) matches this repository's own recent git history.

Each phase's CI/CD verification is separately recorded
(`Phase 1B CI-CD Release Verification`, `Phase 1C CI-CD Release
Verification`), distinct from the later, more complete CI/CD pipeline
implementation described in `docs/operations/CICD_PIPELINE.md` and
[17-cicd-and-deployment.md](17-cicd-and-deployment.md).

## Production access and bootstrap

`Production Read-Only Access Design` and its `Final Hardened Design`
revision, plus `Production Bootstrap Runbook`, establish the read-only
production database access model this manual's own live-verification work
(and the sessions before it) used.

## Staging commissioning investigation → Meenaxy Pharma migration (this session)

`Staging Commissioning Investigation` and its `(Corrected Topology)`
revision, plus `Staging Reference-Data Decision Report`, investigated why
staging's onboarding UI showed empty Device Model / Device Profile
dropdowns, tracing the root cause to `metadata.device_models` and
`config.device_profile_categories` both being empty on staging — a
reference-data gap, not an application bug.

**This session's own work directly followed on from that investigation**:
using production as the read-only authoritative source, it resolved the
device-model naming blocker with real production evidence
(`vendor='Best Energy'`, `model='Eniscope Energy Meter'` — correcting an
earlier test-fixture-based guess of `vendor='Eniscope'`), then built and
executed a transactional, idempotent, business-key-resolved migration
creating Meenaxy Pharma's 22 devices/22 assets/22 identifiers/22
relationships on staging. Two real staging-side surprises were found and
fixed mid-migration, not assumed away:
1. A duplicate `DISTRIBUTIONPANEL` space existed under both `GROUND` and
   `MEZZANINE` floors on staging (production only ever had it under
   `MEZZANINE`) — resolved by qualifying the space lookup by floor, not by
   modifying staging's location data.
2. A live trigger (`metadata.validate_asset_physical_location`, not
   present in any repository migration) required `building_id`/`floor_id`
   to be set explicitly whenever `space_id` was set on an asset row — the
   migration's asset `INSERT` was corrected to populate all three.

Following the migration, all 22 devices were **manually commissioned
through the application's controlled commissioning action** (traced via
`admin.onboarding_audit` to a human portal user, not any automated process
in this session), and real MQTT telemetry began flowing within minutes —
verified end-to-end from raw ingestion through 1-minute/15-minute/hourly
aggregation to the Grafana-facing semantic views, for all 22 devices,
individually traced for 3 representative devices (one per gateway).

This is the first point in the audit trail where staging is confirmed to
be carrying **real, live, commissioned telemetry** rather than empty
metadata scaffolding — every prior Phase 0/1A-1E document's "0 rows" /
"staging has zero devices" observations should be read as historical,
pre-commissioning state, not current fact.

## What this timeline does not cover

Whether Phases 1A-1E's implementation work has been independently
re-verified as still-correct against the current staging schema (only the
specific objects this session directly touched — device/asset metadata,
the aggregation and analytics layers exercised by the pipeline
verification — were re-checked). Treat older phases' specific claims as
historical unless a current document in this manual explicitly re-confirms
them.

## Manual maintenance log

Ongoing entries from this point forward, one per meaningful documentation
change (not every trivial edit). Each entry: date, area, what changed, why,
evidence/source, whether verified live, related code/doc.

### 2026-08-24 — Documentation-maintenance workflow adopted

- **Area**: Manual process itself ([README.md](README.md), [23-known-issues-and-drift.md](23-known-issues-and-drift.md))
- **What changed**: Added a formal source-of-truth priority order and
  contradiction-handling format to the README; added a consolidated
  "Tracked open items" index table to `23-known-issues-and-drift.md`
  covering all 13 currently-known open items (the original 6 documented in
  that file plus 7 more that existed only in topic-specific files —
  `device_profile_categories` DB-trigger gap, possible near-duplicate
  `analytics.v_energy_*` views, uncanonicalized 15min/hourly/daily
  aggregation DDL, two differing "demand" panel semantics, unestablished
  backup/recovery, unestablished admin-portal route inventory, unconfirmed
  onboarding code path).
- **Why**: User directive establishing the manual as a continuously
  maintained artifact, with an explicit requirement to track known open
  items rather than let them go untracked or get silently marked resolved.
- **Evidence/source**: Direct user instruction; cross-checked which items
  were already documented where via targeted grep across
  `docs/platform-manual/` before adding the index (not assumed).
- **Verified live**: No — this was a documentation-structure change only,
  no new system investigation.
- **Related**: None (process change, not a code/schema change).

### 2026-08-24 — Staging Grafana failure investigation (diagnosis only, no fix applied)

- **Area**: Grafana ([12-grafana.md](12-grafana.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md))
- **What changed**: Documented the full, tested list of 18 `grafana_reader`-executable functions (previously described only as "a handful"); added two new tracked issues (`max_connections=25` ceiling, `run_failed_message_recovery_job` actively failing since 21:45).
- **Why**: User reported staging Grafana showing errors across dashboard tiles, live/status tiles, and charts. Investigated read-only, no changes made per explicit instruction.
- **Evidence/source**: Live staging queries via `SET ROLE grafana_reader`, calling 8 of the 18 `analytics.get_grafana_*` functions directly with real Meenaxy data (asset `HVAC_UNIT_PELLET_SECTION_P1_HUV_02`) — all 8 returned correct, current results (fresh as of `22:05` IST). All 13 TimescaleDB background jobs checked via `timescaledb_information.jobs`/`job_stats` — 12 healthy, 1 (`run_failed_message_recovery_job`) failing. `metadata.grafana_organization_map` re-confirmed to have exactly one active row (Meenaxy). `admin.schema_migrations` confirmed migrations 194-197 applied hours before the reported failure.
- **Verified live**: Yes, staging, `ems_admin` + `grafana_reader` impersonation.
- **Related**: `postgres/migrations/194-197_*.sql` (reviewed, ruled out as directly implicated); no code/schema changes made.
- **Outcome**: Could not reproduce the reported failure at the database layer — every query-level, permission-level, and data-freshness check passed. Root cause not established; most likely location is Grafana's own process/container/connection layer, which this session cannot inspect (no SSH/`docker exec`/Grafana API access). See the full diagnostic report in that session's conversation for the complete VERIFIED FACTS / INFERENCE / ROOT CAUSE breakdown.

### 2026-08-24 — Grafana datasource stale-credential bug: root cause found and fixed (code only, not yet deployed)

- **Area**: Grafana provisioning/reconciliation ([12-grafana.md](12-grafana.md))
- **What changed**: `app/src/grafana_client.py` — `provision_organization()` now calls a new `update_datasource()` method (PUT `/api/datasources/uid/ems-timescaledb`) when a tenant's datasource already exists, instead of silently leaving it untouched. `create_datasource()` and `update_datasource()` now share one `_datasource_payload()` builder so their configurations cannot drift apart. `app/tests/test_grafana_client.py` updated accordingly: rewrote the test that asserted the old skip-when-exists behavior, added dedicated update/no-drift regression tests, and added a create-path guard proving `update_datasource` is never called when the datasource is missing.
- **Why**: The staging Grafana failure investigated the same day (previous entry) turned out, once a browser-side Grafana admin check was possible, to be `password authentication failed for user "grafana_reader"` at the Grafana-stored-datasource level, despite the actual Postgres role and `EMS_GRAFANA_DB_PASSWORD` both being independently verified correct. Root cause: existing datasources were never reconciled to the current canonical configuration on any subsequent provisioning run — only ever created once and left alone.
- **Evidence/source**: Direct code inspection of `provision_organization()`'s `if datasource is None: create... ` branch with no `else`; confirmed the only other call site with datasource-touching logic (`grafana_reconciliation_service.py::reconcile_grafana_tenant`) also routes through the same unpatched `provision_organization()`.
- **Verified live**: No — this is a code fix, not yet deployed to staging. Tests run against a locally built `Dockerfile.test` image: `app/tests/test_grafana_client.py` (11/11 passed), full suite (`app/tests`: 980 passed; 57 pre-existing errors, all `psycopg.OperationalError: connection refused` against a local test Postgres this environment doesn't have running — confirmed unrelated to this change, same errors occur in unrelated `test_analytics_grafana_routing.py`/`test_analytics_grafana_electrical_routing.py` files that don't import `grafana_client` at all).
- **Related**: `app/src/grafana_client.py`, `app/tests/test_grafana_client.py`; repair path for the currently-affected Meenaxy tenant is the existing `POST /administration/organizations/{organization_id}/grafana/reconcile` route (`app/src/main.py`) — not yet invoked, pending deployment of this fix.
- **Outcome**: Code fixed and tested locally. Not committed, not pushed, not deployed, and the live Meenaxy datasource has not yet been repaired — all per explicit instruction to report before taking further action.

### 2026-08-24 — Architecture review of the datasource reconciliation fix

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: Added an explicit verification-status breakdown (repo/code vs. automated-test vs. Grafana-docs vs. live-instance evidence) and a minimal recommended staging smoke test (using Grafana's own `/api/datasources/uid/:uid/health` endpoint) to the datasource lifecycle section. No functional code changes — the review confirmed the prior fix's architecture is sound.
- **Why**: User requested a focused architecture/deployment review of the same-day datasource fix, explicitly asking for evidence-level honesty and confirmation that no competing datasource-mutation path exists elsewhere in the repo.
- **Evidence/source**: Repo-wide grep confirmed `provision_organization()` is the only caller of `create_datasource`/`update_datasource`, and the only code path that calls Grafana's `/api/datasources` API at all (`live_main.py`'s Grafana-adjacent routes are WebSocket endpoints for the separate `wisewatts-live-datasource` plugin, unrelated). Grafana's official HTTP API documentation for `PUT /api/datasources/uid/:uid` fetched and checked against our payload shape (confirms convention match; does not explicitly document overwrite-on-PUT semantics).
- **Verified live**: No — documentation-and-code review only, consistent with the explicit "do not deploy/call the reconciliation route/rotate passwords" instruction.
- **Related**: `app/src/grafana_client.py` (unchanged), `docs/platform-manual/12-grafana.md`.
- **Outcome**: No code changes needed. Architecture confirmed sound. One concrete follow-up recommended (the staging smoke test), not yet performed.

### 2026-08-25 — Grafana datasource reconciliation: self-verification and observability fix

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: `app/src/grafana_client.py` — `create_datasource()`/`update_datasource()` now re-read the datasource via `get_datasource_by_uid()` immediately after every write and raise `GrafanaApiError` if it's missing (create) or if `version` didn't strictly increase past the pre-write value (update). `provision_organization()` now returns a structured result dict (`grafana_org_id`, `datasource_action`, `datasource_uid`, `datasource_name`, `datasource_version`) instead of a bare int. All 4 call sites updated (`grafana_provisioning_service.py`; 3 branches of `grafana_reconciliation_service.py`). `app/src/templates/organizations.html` now renders the reconciliation result (previously silently dropped) using the template's existing `alert` convention.
- **Why**: The 2026-08-24 fix (create-vs-update branching) was deployed and live-tested against Meenaxy Pharma (staging, Grafana org 2). The reconciliation route returned `HTTP 200`, but the datasource's `version` stayed at `1` and its health check kept reporting `password authentication failed for user "grafana_reader"`, unchanged, across two separate reconciliation attempts. Investigation (live, this session) ruled out organization-scoping as the cause (`GET /api/orgs`, `GET /api/datasources` under org 1 = `[]`, admin user's own default `orgId` already `2`) — the header-based org-targeting mechanism this app relies on was confirmed working correctly. The DB audit trail that would show the exact prior outcome couldn't be checked (intermittent tunnel connectivity). Root cause identified from first principles instead: Grafana's `PUT` returning a non-error status is not proof of a persisted write — only `version` incrementing is. The code trusted the former.
- **Evidence/source**: Live staging investigation (`GET /api/orgs`, `/api/datasources` under multiple org headers, `/api/user`) via the established SSH-tunnel + `.netrc`/cookie-jar mechanisms from this session. Code inspection of `provision_organization()`, `reconcile_grafana_tenant()`, `reconcile_organization_grafana_tenant()` (main.py route), and `organizations.html` (confirmed via grep: zero references to `grafana_reconciliation`, `result.`, or `if result` before this fix).
- **Verified live**: Partially — the *investigation* (org-scoping, admin user context, datasource inventory) is live-verified. The *fix itself* was verified by the full automated test suite (988 passed, 0 failed, 57 pre-existing environment-only errors unchanged) but **not yet verified against staging Grafana** as of this entry — see the follow-up entry for the deployment/live-verification result, or the `NOT yet verified` line in `12-grafana.md` if that entry doesn't yet exist.
- **Related**: `app/src/grafana_client.py`, `app/src/onboarding/grafana_provisioning_service.py`, `app/src/onboarding/grafana_reconciliation_service.py`, `app/src/templates/organizations.html`, `app/tests/test_grafana_client.py`, `app/tests/test_grafana_reconciliation_service.py`, `app/tests/test_grafana_reconciliation_routes.py`, `app/tests/test_grafana_provisioning_service.py`.
- **Outcome**: Code fixed, tested, documented. Deployed as commit `c40516627588bb671628db0a054de56d29e606b4`, followed by a diagnostic-enrichment commit `7b1ed136f392a4aa2b44754e506468323399ac99` (same day) that added write-response detail — both deployed via the normal `deploy-staging.yml` pipeline.

### 2026-08-25 — Staging credential drift: root-caused, rotated, and ruled out as the sole cause

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: Rotated `grafana_reader`'s PostgreSQL password and staging `app/.env`'s `EMS_GRAFANA_DB_PASSWORD` to a new, identical, generated value (staging only). No code changes in this entry.
- **Why**: A checksum-only comparison (SHA-256 hash and length only — neither value ever displayed, logged, or committed) proved the running admin-portal container's live password did not authenticate as `grafana_reader`. Investigating the credential-provisioning model found *why*: no script or migration in this repository ever sets `grafana_reader`'s password past the `CHANGE_ME_GRAFANA` placeholder in `postgres/ddl/02_roles.sql`, and `EMS_GRAFANA_DB_PASSWORD` is never validated or kept in sync with it — both were manually, independently set at different times with nothing to prevent drift.
- **Evidence/source**: Live, on the staging host, via SSH — password hash comparison and a real `psycopg` connection attempt run from inside the container both before and after rotation; `ALTER ROLE` executed via the same `docker compose exec ... psql -U ems_admin` mechanism `scripts/apply_migrations.sh` already uses (no new access pattern introduced). Full repository search confirmed no `ALTER ROLE`/`ALTER USER` exists anywhere for any role.
- **Verified live**: Yes — new password confirmed authenticating successfully, twice (immediately after rotation, and again after correcting an intermediate wrong-image deploy — see below). **However, re-running the Meenaxy reconciliation with the new, verified-correct password still failed identically** (`version remained 1`, same HTTP 200 write-response body) — this is direct proof the credential was not the sole, or even primary, cause of the reconciliation failure. Root cause of *that* remains open; see `12-grafana.md` for the leading hypothesis (missing `id` field in the PUT payload) and the operational incident (a wrong-image intermediate deploy, caught and corrected via the existing `rollback.yml` `workflow_dispatch` path).
- **Related**: `postgres/ddl/02_roles.sql` (read, not modified — confirms the unmanaged placeholder), `scripts/bootstrap/02_validate_env.sh` (read, not modified — confirms no validation exists for this credential pair), `.github/workflows/rollback.yml` (used, not modified — the non-code-change redeployment path).
- **Outcome**: Credential drift real and fixed. Reconciliation still fails. The investigation continues — this is documented as an open item, not a resolved one.

### 2026-08-25 — Grafana datasource UPDATE payload: added missing `id`/`orgId` fields (code only, not yet deployed or live-verified)

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: `app/src/grafana_client.py` — `update_datasource()` now takes a new `existing_datasource_id` parameter and adds `id` (the existing Grafana datasource's numeric ID) and `orgId` (the Grafana organization ID) to the `PUT /api/datasources/uid/ems-timescaledb` payload, on top of the unchanged fields from `_datasource_payload()`. `provision_organization()` passes `datasource.get("id")` from the record it already fetches via `get_datasource_by_uid()` before deciding create vs. update. `create_datasource()` is unchanged — a newly created datasource has no prior numeric ID to send. `app/tests/test_grafana_client.py` updated: all `update_datasource()` call sites updated for the new parameter, a new test pins `id`/`orgId` presence in the update payload, and the create/update payload-parity test now compares only the common fields (previously a full-payload equality, which would otherwise now fail because the update payload legitimately carries two extra fields).
- **Why**: Grafana 11.6's documented request body for the UID-addressed datasource update endpoint includes `id` and `orgId`; the implementation fixed on 2026-08-24/2026-08-25 (create-vs-update branching, then post-write version verification) never included either, because both fixes built the `PUT` payload from `_datasource_payload()` alone. This was flagged as the leading, untested hypothesis in the 2026-08-25 credential-drift entry above.
- **Evidence/source**: Grafana 11.6 API documentation (request body for `PUT /api/datasources/uid/:uid`); code inspection confirming `_datasource_payload()` is shared by `create_datasource()`/`update_datasource()` and intentionally excludes `id`/`orgId` (a newly created datasource has neither).
- **Verified live**: No. This is a payload-contract correction based on Grafana's documented API, not a live-confirmed fix for the specific staging symptom (`version` not advancing, stale `grafana_reader` password on the Meenaxy datasource). Live staging verification — re-running the Meenaxy reconciliation and confirming `version` advances and the datasource health check passes — is still required and has not yet been performed.
- **Related**: `app/src/grafana_client.py`, `app/tests/test_grafana_client.py`.
- **Outcome**: Code fixed and tested locally (`app/tests/test_grafana_client.py`: 19/19 passed; related onboarding/reconciliation test files: 14/14 passed; full suite: 1047/1047 passed against a local disposable test database). Not yet committed to a deployment, not deployed, and the live Meenaxy datasource has not yet been re-verified — per explicit instruction to stop after implementation and tests, pending review before the live PUT.

### 2026-08-25 — Grafana datasource verification: `version`-based check was itself a bug; replaced with a post-update health check (live-verified root cause)

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: This entry's commit (deployed as `4e2a5e3f765408ce90c25807c82c6eeeda318a18`, confirmed running on staging: `docker inspect` image digest matched CI's pushed digest exactly) was live-verified by running the Meenaxy reconciliation directly via `reconcile_grafana_tenant(portal_user_id=1, organization_id="c4bde6d1-6688-46f0-95df-cdc1a943d17b")` inside the running `ems-admin-portal` container (portal_user_id 1 is the sole active staging portal account, confirmed via `admin.portal_users` through the `ems_admin` DB role, since the app's own DB role has no direct grant on that table). It **failed**: `GrafanaApiError: ... version remained 1 ... Write response was HTTP 200. Body preview: '{"datasource":{"id":1,...}...'` — i.e. the `id`/`orgId` fix from the previous entry did not, by itself, make the reconciliation succeed.
- **Why**: Diagnosing directly on staging (full request/response captured, secrets excluded) showed the PUT's own response body already contained `"message": "Datasource updated"` yet echoed `"version": 1` — Grafana was reporting success while our code's `version`-must-increase gate treated it as failure. To determine which was actually wrong, `pg_stat_activity` was checked (via the `ems_admin` role) immediately after the PUT: it showed a **brand-new** PostgreSQL backend process from `grafana_reader` (~44 seconds old, i.e. opened by Grafana's own connection pool right after the write) that was successfully authenticated and idle. Because PostgreSQL accepts only the single currently-set password for a role, a fresh successful authentication is conclusive: the password Grafana now has stored for this datasource matches the current, independently-verified `EMS_GRAFANA_DB_PASSWORD`. The write **did** take effect; the `version` check was a false negative. Grafana's own HTTP API documentation's example response for this same endpoint independently corroborates this — its sample update response also shows `"version": 1`.
- **Evidence/source**: Live staging investigation via direct SSH access (explicitly authorized this session) — `docker ps`/`docker inspect` for running image verification; a Python script run inside `ems-admin-portal` (via `docker cp` + `docker exec`) using the app's own `GrafanaClient`/`psycopg`/DB-settings to read the datasource, perform the PUT, and check `pg_stat_activity`, printing only non-secret fields (uid/id/orgId/version/database/user/url, a SHA-256 prefix + length of the configured password, and success/failure markers — never the password or a session cookie); `admin.portal_users` queried read-only via `ems_admin` for `portal_user_id`/`is_active` only (no email/PII) after the auto-mode classifier appropriately declined to let this session pick a portal-user identity from the app's own restricted DB role, and the user explicitly authorized a direct, self-determined lookup. Grafana's own HTTP API docs fetched for the same endpoint's example response.
- **Fix**: `app/src/grafana_client.py` — `update_datasource()` no longer takes or checks `previous_version`; after the `PUT` and the existence re-read, it now calls `GET /api/datasources/uid/ems-timescaledb/health` (org-scoped) and raises `GrafanaApiError` unless `status == "OK"`. `create_datasource()` is unchanged. `app/tests/test_grafana_client.py`: replaced the version-based failure regression test with two tests — one pinning failure when the post-update health check reports non-`OK` (the corrected version of the original live-failure regression), one pinning that an unchanged `version` with a healthy check does **not** raise (the false-negative fix); updated the write-response-description and payload-drift tests' fakes to serve a `/health` response; updated `provision_organization()`'s call site and both of its regression tests' fakes for the now-two-argument signature.
- **Verified live**: Yes, fully. Diagnosis: the fresh PostgreSQL session proof described above. Fix: tested locally first (`test_grafana_client.py` 19/19, onboarding/reconciliation suite 14/14, full suite 1048/1048 against a local disposable test database), then deployed to staging via the normal CI/CD pipeline (commit `859493a`, both `CI` and `Deploy to Staging` workflows green, running-image digest confirmed matching CI's pushed digest via `docker inspect`), then the Meenaxy reconciliation was re-run live: `reconcile_grafana_tenant()` returned `{"success": true, "reconciliation_status": "REPAIRED", "datasource_action": "updated", ...}`. Independently re-verified afterward: `GET /api/datasources/uid/ems-timescaledb/health` (org 2) returns `{"status": "OK"}`; the datasource still targets `uid: ems-timescaledb`, `database: ems`, `user: grafana_reader`, `url: timescaledb:5432`; `version` remained `1` (confirming it is not a meaningful signal here, as hypothesized); and a second, independent fresh PostgreSQL session (`pg_stat_activity`, ~36s old) opened by Grafana authenticated successfully as `grafana_reader`.
- **Related**: `app/src/grafana_client.py`, `app/tests/test_grafana_client.py`.
- **Outcome**: Resolved. Root cause of the *reported* reconciliation failure was the verification logic (`version`-must-increase), not the datasource write itself — the write was succeeding all along. The Meenaxy Grafana datasource (`ems-timescaledb`, org 2) is now confirmed healthy end-to-end, live, through the app's own reconciliation path, not just inferred from a side-channel check. No credentials were rotated in this entry; no containers were manually restarted; all changes went through the normal CI/CD pipeline.

### 2026-08-25 — Live-telemetry datasource ("Datasource ffuz5sq2fe9s0a was not found"): plugin never deployed, never provisioned; fixed and made durable

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: Read-only investigation first (separate turn, no changes made) confirmed via three independent live signals — Grafana's own `/api/datasources` (both orgs), `/api/plugins` (0/51 matches), and the staging filesystem (`/opt/ems-platform/grafana/plugins/` missing the plugin entirely) — that the `wisewatts-live-datasource` plugin had never been built/deployed to staging and no matching datasource instance existed anywhere. `ems-asset-overview`'s 8 live/canvas panels reference this datasource by a fixed UID (`ffuz5sq2fe9s0a`) already hardcoded in `grafana/dashboards/core/asset-overview.json`. Immediate recovery: ran the repository's existing `grafana/plugin-src/wisewatts-live-datasource/build-plugin.sh` on staging, installed the artifact to `/opt/ems-platform/grafana/plugins/wisewatts-live-datasource/`, and `docker compose restart grafana` (the only container restarted). Durable fix: `app/src/grafana_client.py` — added `LIVE_DATASOURCE_UID`/`LIVE_DATASOURCE_NAME`/`LIVE_DATASOURCE_TYPE` constants, a `_live_datasource_payload()` builder, `create_live_datasource()`/`update_live_datasource()`, and refactored `create_datasource()`/`update_datasource()` into thin wrappers around new shared `_create_datasource()`/`_update_datasource()` internals so both datasource types share identical create/verify and update/health-check-verify logic (no version-as-success-signal duplication risk). `provision_organization()` now provisions/reconciles both datasources per organization. `app/src/onboarding/grafana_reconciliation_service.py` and `app/src/templates/organizations.html` extended in parallel with the existing `datasource_*` fields. New `scripts/bootstrap/06a_build_grafana_live_plugin.sh`, wired into `scripts/bootstrap.sh` between `06_start_telegraf.sh` and `07_start_grafana.sh`, so a fresh host bootstrap builds the plugin automatically. `deploy_release.sh` (the CI/CD redeploy path) is intentionally left untouched — it deliberately never restarts Grafana on every app deploy, and that boundary was preserved rather than worked around.
- **Why**: The dashboard JSON is shared identically across every tenant organization via `import_dashboards()`, so every org's live-datasource instance must carry the exact same UID for the dashboard to resolve it anywhere — `ffuz5sq2fe9s0a` was adopted as-is (not regenerated) since it was already the working, non-editable-without-cause contract baked into the dashboard, and nothing indicated the value itself was wrong, only that it was never provisioned.
- **Evidence/source**: Live staging investigation via SSH (explicitly authorized) — `docker ps`/filesystem checks, `GET /api/datasources`/`/api/plugins`/`/api/search` via the app's own `GrafanaClient`, repo inspection of `compose.yaml`, `.gitignore`, `.github/workflows/`, `scripts/bootstrap/`, `scripts/release/deploy_release.sh`, and the plugin's own Go source (`datasource.go`) confirming it reads live-stream connection details from the Grafana process environment, not per-instance settings.
- **Verified live**: Plugin build + install + Grafana-only restart + `/api/plugins` recognition — yes (see next entry for the automated-provisioning deploy and reconciliation result).
- **Related**: `app/src/grafana_client.py`, `app/src/onboarding/grafana_reconciliation_service.py`, `app/src/templates/organizations.html`, `app/tests/test_grafana_client.py`, `app/tests/test_grafana_reconciliation_service.py`, `scripts/bootstrap.sh`, `scripts/bootstrap/06a_build_grafana_live_plugin.sh`.
- **Outcome**: See the following entry for final live verification after CI/CD deployment.

### 2026-08-25 — Live-telemetry datasource: automated provisioning deployed and live-verified end-to-end

- **Area**: Grafana ([12-grafana.md](12-grafana.md))
- **What changed**: The dual-datasource provisioning code (previous entry) was committed (`7f8cf8c`, plus a follow-up `52157d4` fixing an executable-bit regression on the new `06a_build_grafana_live_plugin.sh` — Windows git checkouts don't track the bit, and `core.filemode` allowed it to slip in as `100644`), pushed, and deployed via the normal `CI` + `Deploy to Staging` pipeline. Running-image digest confirmed matching CI's build via `docker ps`. The Meenaxy reconciliation was then run live twice in a row through the deployed `reconcile_grafana_tenant()` (portal_user_id 1): the first call returned `live_datasource_action: "created"`; the second, identical call returned `live_datasource_action: "updated"` — proving both the create path and idempotent re-reconciliation.
- **Verified live**:
  - Grafana org 2 now lists exactly two datasources: `ems-timescaledb` (`id:1`, unchanged, `status: "OK"`) and `WiseWatts Live` (`id:2`, `uid: ffuz5sq2fe9s0a`, `type: wisewatts-live-datasource`, `orgId: 2`, `status: "OK"`, message `"WiseWatts live adapter ready"` — confirming the plugin's own `CheckHealth()` found `EMS_GRAFANA_STREAM_TOKEN` configured). Re-checked after the second (idempotent) reconciliation: still exactly two datasources, no duplicates.
  - Full live-telemetry path tested end-to-end, not just inferred: `live-telemetry`'s own `/health` showed `mqtt_connected: true`, actively ingesting (280 messages, most recent ~1 minute old at check time). A direct WebSocket test against `ws://live-telemetry:8090/api/live/grafana/2/assets/{a real Meenaxy asset UUID}/ws` — the exact URL pattern and Bearer-token auth the plugin's `RunStream()` uses — connected successfully and received a real live frame: 50 points, sample point `CURRENT_L1` with a real numeric value and a fresh event timestamp. (The stream token was read via `docker exec ems-grafana printenv` into a shell variable and passed through `docker exec -e`, entirely on the staging host shell; it was never printed to any tool output or included in this document.)
- **Regression**: `app/tests/test_grafana_client.py` (26/26, including 7 new tests: live-datasource create/verify, live-datasource update/health-check-verify including the version-is-not-a-signal case, org-scoping via the generalized `get_datasource_by_uid(org_id, uid)`, both `provision_organization()` branches extended to both datasources, and a dedicated idempotency test); onboarding/reconciliation suite (14/14); full suite (1055/1055, local disposable test database, same CI mechanism).
- **Related**: `app/src/grafana_client.py`, `app/src/onboarding/grafana_reconciliation_service.py`, `app/src/templates/organizations.html`, `app/tests/test_grafana_client.py`, `app/tests/test_grafana_reconciliation_service.py`, `scripts/bootstrap.sh`, `scripts/bootstrap/06a_build_grafana_live_plugin.sh`.
- **Outcome**: Resolved and live-verified end-to-end. `ems-asset-overview`'s live/canvas panels can now resolve `ffuz5sq2fe9s0a` in Grafana org 2, the plugin backend is live and healthy, and real live telemetry is confirmed flowing all the way from MQTT through the live-telemetry service to the exact WebSocket the plugin consumes. New-organization onboarding now provisions both datasources automatically (`test_provision_organization_creates_missing_resources`, extended to assert both). Remaining manual step, documented rather than hidden: if the plugin's *source* changes in the future, an already-bootstrapped host needs `build-plugin.sh` re-run and Grafana manually restarted — `06a_build_grafana_live_plugin.sh` only runs automatically on a fresh bootstrap.

### 2026-08-26 — Job 1077 recovery-pipeline timeout chain: root-caused and fixed across four migrations (201-204)

- **Area**: Telemetry recovery pipeline ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8)
- **What changed**: `telemetry.recover_failed_raw_messages()` (job 1077's worker procedure) was fixed in four separate, deliberately-scoped migrations: **201** bounded its per-candidate supersession check to the candidate's own `[bucket_start, deadline]` window (previously unbounded upper-only); **202** replaced a `telemetry.v_normalized_points` lookup (forcing a chunk-wide `DISTINCT ON` sort, measured 20+ min/candidate on compressed chunks) with a narrowly-scoped `telemetry.normalized_points_for_recovery_candidate()`; **203** added a per-candidate `COMMIT` so a scheduler `max_runtime` cancellation only loses the in-flight candidate, not the whole batch; **204** replaced the remaining `NOT EXISTS` subquery (which still joined through `telemetry.v_rtdata` via ordinary `INNER JOIN`s, letting the planner build an effectively unbounded Hash Anti Join) with a targeted read directly against `telemetry.raw_messages`, plus a corrected `metadata.device_identifiers`-membership identity test.
- **Why**: Job 1077 had been failing on every run since 2026-08-24 21:45 (first flagged in the Grafana investigation the same day, [23-known-issues-and-drift.md](23-known-issues-and-drift.md) §7). Root cause investigated to completion two days later: a real stuck candidate (`raw_message_id=1762912`) had an EXPLAIN-estimated supersession scan of ~467K raw-message rows / ~46.7M JSON-expanded elements / ~701M hash-side rows — explaining the consistent 10-minute timeout.
- **Evidence/source**: Live, read-only EXPLAIN against the exact stuck candidate on staging (no writes, procedure not executed) both before and after each fix; a semantic-equivalence audit caught an unsafe intermediate draft of migration 204 (direct `device_uid` comparison would miss a device with more than one registered MQTT UID — schema-permitted, though zero such devices exist in current staging data) and required the corrected `device_identifiers`-membership form instead.
- **Verified live**: Yes. Local integration suite green for each migration before deploy; staging deploy via the normal CI/CD pipeline; live EXPLAIN re-run against the same candidate after 204 measured 1562.5ms (down from an unbounded scan), with TimescaleDB chunk exclusion confirmed working (1 disk page read, 4 of 5 candidate chunks excluded).
- **Related**: `postgres/migrations/201_recovery_supersession_late_arrival_bound.sql`, `202_recovery_normalized_points_targeted_lookup.sql`, `203_recovery_per_candidate_commit.sql`, `204_recovery_supersession_targeted_window.sql`; `scripts/test/assert_recovery_supersession_late_arrival_bound.sql`, `assert_recovery_normalized_points_targeted_lookup.sql`, `assert_recovery_per_candidate_commit.sql`.
- **Outcome**: All four merged and deployed to staging. Job 1077's `p_limit` was separately raised from 100 to 1000 via `alter_job` (operational config change, no code) once per-candidate cost was empirically measured (~4.5ms cheap path, ~100-260ms supersession path against a 10-minute `max_runtime` budget).

### 2026-08-26 — Job 1000 normalization watermark stall (~15 hours): root-caused, bounded catch-up added, staging recovered

- **Area**: Telemetry normalization ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), Asset Dashboard, known issues (§8)
- **What changed**: Migration 205 added an optional `p_max_window INTERVAL DEFAULT NULL` parameter to `telemetry.load_normalized_points_incremental()`. When supplied (and a previous checkpoint exists), the forward processing boundary is capped to `LEAST(max(raw_messages.received_at), previous_checkpoint + p_max_window)` instead of always advancing to "right now". `NULL` (every existing call site, including job 1000's own wrapper) preserves prior behavior exactly. Because `CREATE OR REPLACE PROCEDURE` does not replace a procedure of a different arity, the migration also explicitly `DROP`s the old one-argument signature first — without this, job 1000's existing single-argument call becomes ambiguous between two overloads instead of remaining compatible (caught by the migration's own regression test before deploy, not in production).
- **Why**: User reported the Asset Dashboard had stopped showing data past ~08:14 on 2026-08-26 while production continued normally. Traced to job 1000 (`telemetry.run_normalization_job`) disabled, stuck at checkpoint `2026-08-26 08:16:37`. Root cause: the procedure always computes its forward boundary as `max(raw_messages.received_at)` in one uncommitted transaction with no forward bound; once the gap between checkpoint and now exceeded the job's 5-minute `max_runtime`, every retry faced an equal-or-wider window with zero possible durable progress — confirmed unrelated to the job-1077 work above (job 1000's failures predated migration 204 by hours).
- **Evidence/source**: `telemetry.pipeline_state` watermark/status history; job-scheduler `job_errors` showing repeated `max_runtime` cancellations; live EXPLAIN-free direct measurement of bounded-chunk durations during the staging recovery (below).
- **Verified live**: Yes, fully. Staging recovery: 32 sequential 30-minute bounded `CALL telemetry.load_normalized_points_incremental(INTERVAL '15 minutes', INTERVAL '30 minutes')` invocations fully drained the backlog from checkpoint `08:16:37` to real-time, every one `SUCCESS` (2-44s each, no errors, no lock contention) — the final chunks naturally tapered to near-zero as the watermark caught up. Downstream `energy_measurements`/`environment_measurements` pipelines auto-advanced on their own independent jobs with zero further intervention needed. Job 1000 then re-enabled (`scheduled=true` only; `schedule_interval`/`max_runtime`/`overlap` config all confirmed unchanged before and after) and observed through 6 subsequent normal 1-minute cycles, all `Success`, watermark continuously advancing.
- **Related**: `postgres/migrations/205_normalization_bounded_catchup_window.sql`, `scripts/test/assert_normalization_bounded_catchup_window.sql`.
- **Outcome**: Resolved and live-verified. Backlog catch-up was explicitly NOT started until user authorization was given for each phase (deployment → staging verification → catch-up execution → job 1000 re-enable), per the incident's own safety constraints.

### 2026-08-26/27 — Job 1077 population-selection simplified to retention age; one-time backlog cleanup; downstream aggregation gaps found and repaired

- **Area**: Telemetry recovery pipeline, analytics aggregation ([10-analytics.md](10-analytics.md), [11-aggregation.md](11-aggregation.md)), known issues (§8)
- **What changed** (three related but separately-scoped pieces of work):
  1. **Migration 206**: `telemetry.recover_failed_raw_messages()`'s candidate classification changed from a `raw_messages` existence probe to an age comparison against the live `telemetry.raw_messages` retention policy, read dynamically from `timescaledb_information.jobs` (`proc_schema='_timescaledb_functions'`, `proc_name='policy_retention'` — stable identifiers, never hard-coded 48 hours). A candidate older than the cutoff is now marked `PERMANENT_FAILURE` immediately, skipping the expensive supersession logic entirely. Migration 204's supersession subquery, migration 203's per-candidate `COMMIT`, `p_limit`, and ordering are all unchanged.
  2. **One-time staging data cleanup** (not a migration — a direct, transactional, evidence-gated data operation): 1,060 `raw_message_failures` rows whose `raw_received_at` was both past the retention cutoff **and** confirmed absent from `raw_messages` were marked `PERMANENT_FAILURE`, using the exact same `last_replay_error` text and (deliberately) leaving `resolution_method`/`next_replay_at` exactly as the procedure's own retention-quarantine branch would.
  3. **Downstream aggregation repair**: investigation of continued "Demand Profile"/"Energy Consumption Trend" Grafana panel gaps (STAGING) traced both to residual effects of job 1000's earlier stall, in two separate downstream layers neither of which self-heals (see §8 for full detail): `analytics.demand_intervals` (1,320 stale `NO_DATA` rows corrected via the existing `analytics.calculate_demand_window()` helper) and `analytics.energy_consumption_1min` (19,455 rows backfilled via the existing `analytics.refresh_energy_consumption_1min(p_from,p_to)` function). `analytics.energy_consumption_5min` was investigated and found to be intentionally, permanently empty for this device fleet (capture-interval routing, not a defect) — not touched.
- **Why**: Migration 206 was requested to stop job 1077 wasting its 10-minute runtime budget re-processing candidates already provably outside the recoverable retention window (before this fix, ~1,060 of ~1,964 "recoverable" candidates were already dead but kept re-entering the expensive path because the daily retention-drop job hadn't physically swept them yet). The downstream aggregation repair was requested after confirming, via direct exact-panel-query execution against staging, that the Grafana panels were reading genuinely stale/gapped data — not a Grafana, panel-SQL, or semantic-layer bug.
- **Evidence/source**: All staging queries and repairs run read-only-first (preview before any write), then transactionally with before/after row counts and duplicate checks; every repair reused an existing, already-deployed function rather than introducing new business logic. Full evidence trail (exact SQL, row counts, panel-query re-execution) is in this session's own record; summarized in [23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8.
- **Verified live**: Yes. Migration 206: local integration suite green, staging deploy via normal CI/CD, deployed signature/checksum/ledger confirmed. Data cleanup and both aggregation repairs: before/after counts reconciled exactly (e.g. demand: `NO_DATA` 1,602→282, `VALID` 2,500→3,814, total row count unchanged; energy: table count +19,455 matching the function's own return value and source row count exactly, zero duplicates). Both Grafana panels re-verified via their exact panel SQL post-repair showing continuous data; a follow-up full health check confirmed jobs 1000/1078/1079/1080 all running normally with no new gap.
- **Related**: `postgres/migrations/206_recovery_retention_age_population.sql`, `scripts/test/assert_recovery_retention_age_population.sql`; `analytics.calculate_demand_window()`, `analytics.refresh_energy_consumption_1min()` (both pre-existing, unmodified).
- **Outcome**: Resolved and live-verified. The underlying architectural gap — none of `demand_intervals`/`energy_consumption_1min`/`5min`'s populating jobs can self-heal after a future multi-hour outage without another manual repair like this one — remains open and undocumented as a fix; two options were identified (bounded manual-backfill parameters, or bounded-reprocessing `ON CONFLICT DO UPDATE` finalization) but neither was approved or implemented.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 0: energy/environment routing bounded catch-up (migration 207)

- **Area**: Telemetry domain routing ([07-telemetry-pipeline.md](07-telemetry-pipeline.md) "Watermarks and bounded catch-up"), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8 / item 16)
- **What changed**: Migration 207 adds an optional `p_max_window INTERVAL DEFAULT NULL` parameter to `telemetry.load_energy_measurements_incremental()` and `telemetry.load_environment_measurements_incremental()`, mirroring migration 205's fix for `telemetry.load_normalized_points_incremental()` exactly (including the `DROP PROCEDURE IF EXISTS …(interval)` first, because `CREATE OR REPLACE` cannot change a procedure's arity). When `p_max_window` is `NULL` the forward boundary is computed exactly as before (`max(telemetry.normalized_points.platform_received_at)`); when supplied and a previous checkpoint exists, it is additionally capped to `LEAST(window_end, previous_checkpoint + p_max_window)` via a single `LEAST(…)` immediately after the existing `window_end`/`NULL` check. The scheduled wrappers `telemetry.run_energy_routing_job` and `telemetry.run_environment_routing_job` now derive `v_max_window` (default `INTERVAL '2 hours'`, overridable via `config.max_window`) and always pass it to the loader — they never pass `NULL`. The migration also merges `"max_window": "2 hours"` into each job's live `config` via `alter_job` (idempotent, skipped if the job is not registered). Everything else in both procedure bodies — the `pg_try_advisory_xact_lock`, the `pipeline_state` RUNNING/SUCCESS/FAILED/SKIPPED_LOCKED/NO_SOURCE_DATA handling, the checkpoint keying on `normalized_points.platform_received_at`, the `LEAST(p_overlap, 1 minute)` clamp, the `window_events`/`full_resolution`/`resolve_site_capture_bucket` routing and register/environment calculation SQL, the `num_nonnulls` filter, the `capture_bucket_correction_deadline` guard, the `ON CONFLICT (bucket_start,device_id) DO UPDATE` (energy) / `DO NOTHING` (environment) write semantics, and the `EXCEPTION WHEN OTHERS → set FAILED → RAISE` contract — is reproduced verbatim from the currently deployed definition.
- **Why**: The EMS Analytics Platform Phase 2 Foundation investigation (`Audit/EMS Analytics Platform — Phase 2 Foundation Implementation-Planning Design (STAGING, design only).txt`) proved from the deployed procedure bodies that jobs 1001 and 1012 retained the exact unbounded-forward-boundary-in-one-uncommitted-transaction shape that stalled job 1000 before migration 205 — a latent repeat of the 2026-08-26 incident one hop downstream, not yet triggered only because no `normalized_points` burst that large has reached those two loaders since deploy. Phase 0 removes that latent risk before it fires; with a bounded scheduled wrapper an unattended multi-hour backlog self-drains over successive 1-minute runs (≈8 runs for a 15-hour gap) instead of stalling.
- **Scope boundary**: Phase 0 only. It does **not** touch job schedule / `max_runtime` / `max_retries` / `retry_period` (unchanged at 1 minute / 5 minutes / 3 / 1 minute; `config.overlap` stays `15 minutes`, so `assert_job_schedule_canonical` still passes), `telemetry.load_normalized_points_incremental()` or job 1000, any analytics-tier job (1078/1079/1080/1099/1102/1105/1107), `demand_intervals` finalization, retention, compression, CAGGs, Grafana, or any downstream schema/function. No new table, queue, or service.
- **Evidence/source**: Migration body generated by transforming the live `pg_get_functiondef()` output of both loaders with exactly two textual edits each (signature + the `LEAST` cap), so the ~850-line energy body is not hand-transcribed. Local disposable-database integration suite green: migration 207 applies cleanly (same harmless "already a transaction in progress" warnings as 205/206), all 8 assertion groups in the new `scripts/test/assert_energy_environment_routing_bounded_catchup_window.sql` pass (A: `NULL` preserves the unbounded boundary for both loaders; B/G: both scheduled wrappers cap a burst at exactly checkpoint + 2 hours by default, `config.max_window` override honoured; C: checkpoint advances to exactly `min(max, checkpoint + p_max_window)`, and stops at the true max when the requested horizon exceeds available data; D: an injected failure at the checkpoint-advance step rolls the checkpoint back to its pre-call value, `FAILED`/`RAISE` handler intact; E: a subsequent bounded run resumes from the previous checkpoint, +2h then +4h; F: re-running the identical bounded window is idempotent; H: live catalog check that no existing routing/calculation fragment changed), `assert_job_schedule_canonical` still passes, and the pre-207 one-argument signatures are gone (no overload ambiguity).
- **Verified live**: **Not yet.** Repository + local disposable-database only, pending review/approval of Phase 0 before staging deploy via the normal CI/CD pipeline (per the phased-approval discipline this work follows).
- **Related**: `postgres/migrations/207_energy_environment_routing_bounded_catchup_window.sql`, `scripts/test/assert_energy_environment_routing_bounded_catchup_window.sql` / `.sh`, `scripts/test/run_integration_environment.sh`, `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — Phase 2 Foundation Implementation-Planning Design (STAGING, design only).txt`.
- **Outcome**: Implemented and locally validated; awaiting review. Phase 0b (analytics job hardening — advisory locks, `pipeline_state` rows, finite `max_runtime`/`max_retries`, config validator, `assert_job_schedule_canonical` extension) is the next PR and has **not** been started.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 0b: analytical-tier job hardening (migration 208)

- **Area**: Analytics aggregation jobs ([10-analytics.md](10-analytics.md), [11-aggregation.md](11-aggregation.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8 / items 9, 16)
- **What changed**: Migration 208 makes the seven analytical-tier background jobs **safe to run** before their window semantics are changed in a later phase — it does **not** add watermark or bounded catch-up processing. For `analytics.run_energy_consumption_{1min,5min,15min,hourly,daily}_job`, `analytics.run_demand_calculation_job`, and `telemetry.run_environment_daily_job`:
  1. **`config.assert_analytical_lookback_job_config(jsonb)`** — the first `check_config` validator in the platform (the TimescaleDB `bgw_job.check_schema`/`check_name` hook, previously unused). Wired onto all seven jobs; rejects, at `add_job`/`alter_job` time, a config that is not a JSON object, carries a key other than `lookback`/`max_catchup_window`, or whose interval value is not positive and finite.
  2. **Seven `telemetry.pipeline_state` rows** (`energy_consumption_1min`/`5min`/`15min`/`hourly`/`daily`, `demand_intervals`, `environment_daily`) — reusing the existing table and its existing columns unchanged (`INSERT … ON CONFLICT DO NOTHING`, idempotent). `last_received_at` is left `NULL` and is never read or written — **this migration implements no watermark**. The rows exist for operational state/observability and to give the later watermark phases the same transactional pattern.
  3. **`CREATE OR REPLACE` of the seven wrappers**, each wrapping its byte-for-byte-preserved processing core (the `config.lookback` parse/validate, the exact `date_trunc`/`date_bin`/`clock_timestamp` `v_to` computation, and the `PERFORM`/`CALL` of `analytics.refresh_energy_consumption_*` / `analytics.refresh_demand_analytics` / `telemetry.refresh_environment_daily`) with the same advisory-lock + `pipeline_state` pattern the telemetry loaders use: `pg_try_advisory_xact_lock(hashtextextended('<proc>', 0))` on entry → `SKIPPED_LOCKED` + `RETURN` on miss (ahead of the `RUNNING` write and the processing path); `RUNNING` once locked; `SUCCESS` on completion, or `NO_SOURCE_DATA` when the full-recompute refresh reported zero affected rows; `EXCEPTION WHEN OTHERS` → `FAILED` + `RAISE`, identical to the telemetry loaders. None of the `refresh_*` functions are touched.
  4. **`alter_job` on each of the seven**: `max_runtime` → `5 minutes` (1min/5min/15min tiers) or `10 minutes` (hourly/daily/demand/environment_daily tiers), `max_retries` → `3`, `retry_period` → `5 minutes`, `scheduled` → `TRUE`, `check_config` → the validator. `schedule_interval` and `config` are **not** passed and are preserved exactly (cadence unchanged; `config` stays `{"lookback": "..."}`).
- **Why**: Phase 2 Foundation design finding **P-C1** — none of the seven wrappers took an advisory lock or recorded any state, and every job had `max_runtime = 00:00:00` (unlimited) and `max_retries = -1` (infinite). A run that overran its schedule interval could be joined by a second concurrent invocation processing the same window — for `run_demand_calculation_job`, a double-apply of the provisional `analytics.demand_state` accumulator or a row-lock deadlock. This hardening is a prerequisite for safely deploying the child-watermark logic in a later phase (a watermark advanced by two overlapping workers is exactly the false-advance that phase must prevent).
- **Scope boundary**: No child watermark, bounded catch-up window, cascade dependency, `demand_intervals` `ON CONFLICT DO UPDATE`, reconciliation, `v_pipeline_health` view, or move of the `add_job` calls into `postgres/jobs/`. The last is deliberately deferred: migrations 013/028/031/179/181/183/184 register these jobs, several with a bare unguarded `add_job`, and are checksum-locked; a canonical-layer registration running before them would double-register. Fresh-deploy reproducibility is already provided by those migrations and is now **enforced** by the extended `scripts/test/assert_job_schedule_canonical.sql`.
- **Evidence/source**: Local disposable-database integration suite green. Migration 208 applies cleanly (same harmless "already a transaction in progress" warnings as 205–207). New `scripts/test/assert_analytics_job_hardening.sql` passes (A: 7 `pipeline_state` rows exist, `last_received_at` all NULL; B: finite `max_runtime` 5m/10m + `max_retries` 3 + `retry_period` 5m + `check_config` wired on all 7; C: each wrapper records `RUNNING → SUCCESS`/`NO_SOURCE_DATA` with consistent timing and no watermark; D: an injected failure at the terminal `pipeline_state` write rolls the whole run back — state left at its pre-call value, `EXCEPTION → FAILED → RAISE` handler intact; E: advisory-lock + `SKIPPED_LOCKED` + `RETURN` branch present on each; F: validator accept/reject cases; G: each wrapper's `date_trunc`/`date_bin`/`clock_timestamp` window and `refresh_*` call are unchanged). New cross-session `scripts/test/assert_analytics_job_self_overlap.sh` passes (a background session holds the job's advisory `xact` lock; a foreground `CALL` of the same job returns `SKIPPED_LOCKED`, advances no state, writes zero target rows, and — for demand — leaves `analytics.demand_state` byte-identical; no deadlock). `assert_job_schedule_canonical` (extended to the 7 analytical jobs) passes. Full `run_integration_environment.sh` green with migrations 207 + 208 applied.
- **Verified live**: **Not yet.** Repository + local disposable-database only, pending review/approval of Phase 0b before staging deploy. Staging `job_stats.last_run_duration` / `job_history` percentiles for the seven jobs should be sampled before deploy to confirm the 5m/10m `max_runtime` values leave adequate headroom at production fleet size.
- **Related**: `postgres/migrations/208_analytics_job_hardening.sql`, `scripts/test/assert_analytics_job_hardening.sql` / `.sh`, `scripts/test/assert_analytics_job_self_overlap.sh`, `scripts/test/assert_job_schedule_canonical.sql` (extended), `scripts/test/run_integration_environment.sh`, `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — Phase 2 Foundation Implementation-Planning Design (STAGING, design only).txt` §Q Phase 0b.
- **Outcome**: Implemented and locally validated; awaiting review. The next PR is Phase 1 (child watermarks for `energy_consumption_1min`/`5min`), which has **not** been started and requires explicit approval.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 1 (1/4): energy-consumption cascade watermarks (migration 209)

- **Area**: Analytics aggregation ([10-analytics.md](10-analytics.md), [11-aggregation.md](11-aggregation.md)), telemetry pipeline watermarks ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8 / item 16)
- **What changed**: Migration 209 replaces the `now() - <fixed lookback>` window derivation in the five `analytics.run_energy_consumption_{1min,5min,15min,hourly,daily}_job` wrappers with a **parent-availability + child-checkpoint + bounded-catch-up** model. Per run: `v_ckpt = telemetry.pipeline_state(child).last_received_at` (read `FOR UPDATE`); `v_parent = parent_available_through` read live — `analytics.cagg_available_through('telemetry.ca_energy_1min'/'ca_energy_5min')` for the two native tiers, `LEAST(cp('energy_consumption_1min'), cp('energy_consumption_5min'))` for 15min, `cp('energy_consumption_15min')` for hourly **and** daily (daily reads `analytics.energy_consumption_15min` directly, not the hourly tier); `v_start = COALESCE(v_ckpt, now_binned - lookback)` where `lookback` is now only the **first-run floor**; `v_to = LEAST(now_binned, v_parent, v_start + max_catchup_window)`; if `v_parent IS NULL` or `v_to <= v_start` the run records `NO_SOURCE_DATA` and does **not** advance; otherwise it calls the **unchanged** `analytics.refresh_energy_consumption_*` function over `[v_start - overlap, v_to)` and then advances `last_received_at = v_to` as the last write of the run's single transaction. `EXCEPTION WHEN OTHERS → FAILED → RAISE` ⇒ the TimescaleDB job runner aborts the whole job transaction, so a failed / cancelled / timed-out run leaves the checkpoint exactly at its pre-run value. Adds `analytics.cagg_available_through(regclass)` (catalog-only CAGG materialization-watermark helper; returns NULL when a CAGG has never materialized real data). Extends `config.assert_analytical_lookback_job_config` to accept & validate `overlap` and `reconcile_window`. Each of the five jobs' `config` gains `max_catchup_window` / `overlap` / `reconcile_window` (per-tier values from the readiness audit; `reconcile_window` is stored now and consumed by migration 212); `lookback`, `schedule_interval`, `max_runtime`, `max_retries`, `retry_period`, and the migration-208 advisory-lock / `SKIPPED_LOCKED` / `RUNNING` / `FAILED` scaffolding are preserved verbatim.
- **Why**: Phase 1 Implementation-Readiness Audit — the wall-clock lookback anchors each child's window to `now()`, so historical source data that arrives outside the fixed window after an upstream outage/burst is permanently missed. The watermark model makes each child causally bounded by what its parent has actually made available, drains a backlog in bounded ordered idempotent slices, and cannot skip its own failed interval (`v_to` is anchored to `v_ckpt + max_catchup_window`, and `v_ckpt` only moves on success).
- **Empty 5-minute tier**: `analytics.energy_consumption_5min` is empty by design for the 60s fleet, but `telemetry.ca_energy_5min` (the CAgg) has data, so `cagg_available_through` advances, the 5min wrapper's `v_to` advances, `refresh_energy_consumption_5min` returns 0 rows → `NO_SOURCE_DATA` **but `last_received_at` still advances**. `LEAST()` also ignores NULLs, so a not-yet-run 5min checkpoint falls back to the 1min checkpoint. The 15-minute cascade never stalls on the empty tier.
- **CAGG watermark caveat (readiness-audit finding C3)**: the materialization watermark can sit ahead of real data during an upstream stall (the refresh policy advances it while materializing empty buckets). Migration 209 stores each CAGG-fed tier's `reconcile_window` = that CAgg's `start_offset` (2 days for `ca_energy_1min`, 7 days for `ca_energy_5min`); migration 212's bounded trailing reconciliation consumes it. 209 does **not** implement reconciliation.
- **Scope boundary**: No `refresh_energy_consumption_*` calculation function, schema, retention, compression, CAGG policy, Grafana object, or routing/normalization change. Demand (`ON CONFLICT DO UPDATE` + watermark) is migration 210; `environment_daily` watermark is migration 211; reconciliation + `pipeline_reconciliation_log` + `v_pipeline_health` are migration 212 — **none started**.
- **Evidence/source**: Local disposable-database integration suite green. Migration 209 applies cleanly (same harmless "already a transaction in progress" warnings as 205–208). New `scripts/test/assert_energy_consumption_cascade_watermarks.sql` passes all 17 assertions (A initial watermark; B normal forward; C/E parent-far-ahead → advance bounded to exactly one `max_catchup_window`; D parent unavailable → no advance, `NO_SOURCE_DATA`; F 3 successive bounded runs drain a backlog; G/H failure → checkpoint unchanged; I identical re-run idempotent; J self-overlap guard intact; K 1min→15min bounded by `LEAST(cp_1min, cp_5min)`; L empty 5min does not block 15min and still advances its own checkpoint; M 15min→hourly bound; N 15min→daily bound; O the 1min bound is the CAgg watermark, not `now()`; P tenant isolation preserved; Q wrapper-driven 15min output == direct `refresh_energy_consumption_15min` output for the same window). `assert_analytics_job_hardening` (migration 208) was updated for 209 compatibility (its window-derivation checks for the 5 energy wrappers were superseded; the demand + `environment_daily` checks are unchanged) and `assert_job_schedule_canonical` remains green. Full `run_integration_environment.sh` green with migrations 207 + 208 + 209.
- **Verified live**: **Not yet.** Repository + local disposable-database only, pending review/approval of Phase 1 (1/4) before staging deploy. Staging `job_stats.last_run_duration` and the `ca_energy_1min`/`ca_energy_5min` materialization watermarks should be sampled before deploy (readiness-audit §H #3–5) to confirm the per-tier `max_catchup_window` sizes.
- **Related**: `postgres/migrations/209_energy_consumption_cascade_watermarks.sql`, `scripts/test/assert_energy_consumption_cascade_watermarks.sql` / `.sh`, `scripts/test/assert_analytics_job_hardening.sql` (updated), `scripts/test/run_integration_environment.sh`, `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — Phase 1 Implementation-Readiness Audit (STAGING, audit only).txt`.
- **Outcome**: Implemented and locally validated; awaiting review. Migrations 210 (demand), 211 (`environment_daily`), 212 (reconciliation) have **not** been started and each requires explicit approval.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 1 (2/4): demand watermark + status-guarded re-finalization (migration 210)

- **Area**: Analytics aggregation ([10-analytics.md](10-analytics.md)), demand pipeline, telemetry pipeline watermarks ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8 / item 16)
- **What changed**: Migration 210 makes demand finalization **self-healing** and **watermark-driven**, and replaces the write-once `INSERT … ON CONFLICT DO NOTHING` on `analytics.demand_intervals` with a status-guarded upsert.
  1. **`analytics.refresh_demand_analytics`** gains two optional trailing parameters `p_finalize_from TIMESTAMPTZ DEFAULT NULL`, `p_finalize_to TIMESTAMPTZ DEFAULT NULL` that bound **only** the historical finalization loop to an explicit `[COALESCE(p_finalize_from, p_now - p_lookback), COALESCE(p_finalize_to, p_now))` window. `NULL/NULL` — every legacy two-argument caller (`assert_asset_demand_automatic_decoupling`, the migration-208 wrapper) — reproduces the exact pre-210 window. The live `analytics.demand_state` block is **unchanged** and stays keyed to `p_now`, so a bounded historical catch-up can never turn into repeated accumulation in `demand_state`. `analytics.calculate_demand_window()` and `analytics.resolve_demand_interval()` are **not touched** — the demand mathematics and quality lattice are byte-for-byte unchanged. Because `CREATE OR REPLACE` cannot change a procedure's arity, the two-argument signature is `DROP`ped first (migration-205 pattern); owner / `REVOKE` / `GRANT` / `COMMENT` are re-issued for the four-argument signature.
  2. **The finalization write** becomes a scope-branched status-guarded upsert (required because `analytics.demand_intervals` carries two *partial* unique indexes: `uq_demand_intervals_site_scope` `WHERE scope_type='SITE'` and `uq_demand_intervals_asset_scope` `WHERE scope_type='ASSET'` — an unqualified `ON CONFLICT` cannot infer the arbiter). Per scope: `INSERT … ON CONFLICT (<site_id|asset_id>, interval_start, demand_policy_id) WHERE scope_type = '<SCOPE>' DO UPDATE SET <all computed columns>, finalized_at = clock_timestamp() WHERE analytics.demand_intervals.quality_status <> 'VALID' AND (quality_status | demand_kw | demand_kva | coverage_percent | observed_observations IS DISTINCT FROM the persisted value)`. ⇒ a finalized `VALID` interval is **frozen** (never downgraded or rewritten); a `NO_DATA` / `INCOMPLETE` / other non-`VALID` row is re-finalized only when `calculate_demand_window` (rules unchanged) now yields a materially different result — so a later-arriving valid source promotes `NO_DATA`/`INCOMPLETE` → `VALID` — and an unchanged recompute writes nothing, so `finalized_at` does not churn.
  3. **`analytics.run_demand_calculation_job`** becomes watermark-driven (migration-209 pattern): `v_ckpt = telemetry.pipeline_state('demand_intervals').last_received_at` (`FOR UPDATE`); `v_grace = make_interval(secs => max(config.site_demand_policies.late_arrival_tolerance_seconds WHERE is_enabled) + 300)` (≤ 3900 s by the table CHECK); `v_parent = LEAST(max(telemetry.energy_measurements.bucket_start), max(telemetry.normalized_points.event_time))` — the actual closed-bucket source frontier, **not** a routing `pipeline_state` watermark (which is ingest time); `v_start = COALESCE(v_ckpt, clock_timestamp() - v_lookback)` (`lookback` is now only the first-run floor); `v_to = date_bin('15 minutes', LEAST(clock_timestamp() - v_grace, v_parent, v_start + v_max_catchup_window))`. If `v_parent IS NULL` or `v_to <= v_start` the run records `NO_SOURCE_DATA` (or `SUCCESS` if `v_to = v_ckpt`) and does **not** advance. Otherwise it `CALL`s `analytics.refresh_demand_analytics(clock_timestamp(), v_lookback, v_start - v_overlap, v_to)` and then advances `last_received_at = v_to` as the last write of the run's single transaction. `EXCEPTION WHEN OTHERS → FAILED → RAISE` ⇒ the TimescaleDB job runner aborts the whole job transaction, so a failed / cancelled / timed-out run leaves the checkpoint exactly at its pre-run value. The `v_grace` clamp keeps the checkpoint from leaping over the finalize loop's own internal grace-skipped region (which would otherwise leave a permanent hole below the watermark).
  4. The demand job's `config` gains `max_catchup_window` (`6 hours`), `overlap` (`30 minutes`), and `reconcile_window` (`6 hours`; stored now, consumed by migration 212 only). `lookback` (`3 hours`), `schedule_interval` (`1 minute`), `max_runtime` (`10 minutes`), `max_retries` (`3`), `retry_period` (`5 minutes`), the migration-208 `check_config` validator, and the migration-208 advisory-lock / `SKIPPED_LOCKED` / `RUNNING` / `FAILED` scaffolding, `SECURITY INVOKER`, `search_path`, and owner `ems_admin` are all preserved verbatim.
- **Why**: `analytics.demand_intervals` was finalized write-once, so a 15-minute interval finalized `NO_DATA` / `INCOMPLETE` while upstream was stalled was never reconsidered even after real source data later arrived; and `run_demand_calculation_job` re-scanned a fixed `now()-3h` window every minute, so nothing older than 3 h was ever revisited. Watermark + bounded catch-up drains a demand backlog in bounded ordered idempotent slices and cannot skip its own failed interval; the status-guarded upsert repairs a historical hole using the *existing* quality calculation without rewriting history or churning `finalized_at` on stable rows.
- **Scope boundary**: `analytics.calculate_demand_window`, `analytics.resolve_demand_interval`, `analytics.resolve_demand_capability`, the live `analytics.demand_state` block and its two `DELETE`s, the `demand_state` / `demand_intervals` schemas, demand quality rules, tenant isolation, demand interval grain, and any CAGG / retention / compression / Grafana object / job schedule / cadence are **untouched**. `environment_daily` watermark is migration 211; reconciliation + `pipeline_reconciliation_log` + `v_pipeline_health` are migration 212 — **none started**.
- **Known limitation (surfaced for review)**: the upsert guard is `quality_status <> 'VALID'` (per the approved audit §F3). This also *permits* an `INCOMPLETE` → `NO_DATA` transition, which is unreachable in the real pipeline (source rows in `telemetry.energy_measurements` / `telemetry.normalized_points` are never deleted, so a bucket that once had observations cannot recompute to `observed_observations = 0`). A one-line hardening (`AND NOT (demand_intervals.quality_status <> 'NO_DATA' AND EXCLUDED.quality_status = 'NO_DATA')`) is available if a stricter monotonic guarantee is wanted.
- **Evidence/source**: Local disposable-database integration suite green. New `scripts/test/assert_demand_watermark_refinalization.sh` (self-contained heredoc, `assert_asset_demand_automatic_decoupling.sh` pattern) (rollback-only; holds the demand advisory lock for the whole transaction) covers: `T-D` no parent → `NO_SOURCE_DATA`, no advance; `T-Refin-B` `NO_DATA` → `VALID` on late source with `finalized_at` advance; `T-Refin-A` unchanged `VALID` window re-run → no write, `finalized_at` stable; `T-Refin-D` still-`NO_DATA` re-run → unchanged; `T-Refin-INC` seeded `INCOMPLETE` + full source → guard promotes to `VALID`; `T-Refin-scope` SITE + ASSET rows repaired in one call, no `ON CONFLICT` inference error, no cross-scope leak; `T-K` tenant isolation (org B repaired, org A `VALID` row byte-identical incl. `finalized_at`, `organization_id` consistent); `T-Refin-E` migration-212-style trailing reconcile repairs a hole **without** moving `last_received_at`; `T-WM-A` first run advances to `date_bin(15m, LEAST(now-grace, parent, start+max_catchup))`; `T-WM-C` per-run advance bounded to `checkpoint + max_catchup_window`, not parent/now; `T-WM-fail` injected failure on the advance write → `RAISE` propagates, checkpoint intact; `T-DS` a 6 h catch-up leaves only current-interval rows in `analytics.demand_state`; `T-lock` wrapper retains the migration-208 concurrency scaffolding. `assert_analytics_job_hardening` updated for 210 (demand wrapper check now asserts the 4-arg bounded call + watermark advance + 15-min catch-up derivation; `demand_intervals` moved out of the "no watermark" list, leaving only `environment_daily`). `assert_asset_demand_automatic_decoupling` and `assert_job_schedule_canonical` remain green unchanged. The transformed `refresh_demand_analytics` body was generated deterministically from the deployed definition — line-level delta `+88 / -10` lines, exactly the six audited mechanical edits (window-var derivation, `v_max_n` from the window, loop step anchor, `EXIT` bound, defensive `CONTINUE WHEN … IS NULL`, and the scope-branched upsert); everything else reproduced verbatim.
- **Verified live**: **Not yet.** Repository + local disposable-database only, pending review/approval of Phase 1 (2/4) before staging deploy. A measured single 6-hour demand catch-up cost on staging (`job_stats` / a manual bounded `refresh_demand_analytics` call) should be sampled before deploy; if it exceeds ~2–3 min, reduce `max_catchup_window` (config only, no code) so the backlog drains over more, smaller runs.
- **Related**: `postgres/migrations/210_demand_watermark_refinalization.sql`, `scripts/test/assert_demand_watermark_refinalization.sh` (self-contained heredoc, `assert_asset_demand_automatic_decoupling.sh` pattern), `scripts/test/assert_analytics_job_hardening.sql` (updated), `scripts/test/run_integration_environment.sh`, `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — Migration 210 Pre-Implementation Audit (Demand Watermark + Re-finalization, STAGING, audit only).txt`.
- **Outcome**: Implemented and locally validated; awaiting review. Migrations 211 (`environment_daily`), 212 (reconciliation) have **not** been started and each requires explicit approval.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 1 (3/4): environment_daily watermark (migration 211)

- **Area**: Telemetry pipeline watermarks ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), analytics aggregation ([10-analytics.md](10-analytics.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8 / item 16)
- **What changed**: Migration 211 makes `telemetry.run_environment_daily_job` (job `run_environment_daily_job`) **watermark-driven with bounded catch-up over site-local days**, replacing the fixed `now() - 8 days` wall-clock window it re-scanned every hour.
  1. **`telemetry.refresh_environment_daily(p_from, p_to)` is NOT modified.** Verified from the deployed body, it already: reads `telemetry.environment_measurements` **directly** by `bucket_start` (**never** a `ca_environment_*` CAGG); localises each bucket to `metadata.sites.timezone` (IANA, DST-aware) and groups by `(device_id, site-local calendar day)`; emits a `(device, local-day)` row **only** where `local_day_end > p_from AND local_day_end <= p_to` (complete site-local days only — today's partial day is never written; a day/device with no source rows produces **no row**; there is no `NO_DATA` sentinel and no `quality_status` column); scans source back to `p_from - INTERVAL '1 day'`; writes `INSERT … ON CONFLICT (device_id, bucket_start) DO UPDATE` (single unique index — already an upsert).
  2. **`telemetry.run_environment_daily_job` new window derivation** (migration-208 advisory-lock / `RUNNING` / `FAILED→RAISE` scaffolding, `SECURITY DEFINER`, `search_path`, owner `ems_admin` all preserved verbatim; only the DECLARE block and the "Window derivation" section change): `v_ckpt = telemetry.pipeline_state('environment_daily').last_received_at` (`FOR UPDATE`); `v_grace = make_interval(secs => COALESCE(max(late_arrival_tolerance_seconds) FILTER (WHERE is_enabled), 900) + COALESCE(max(capture_interval_seconds) FILTER (WHERE is_enabled), 900) + 300)` over **`config.telemetry_capture_policies`** — **not** `config.site_demand_policies` (that governs demand; different table, different CHECK ceiling) — so a local day is not finalized while any of its `environment_measurements` buckets could still be corrected (`telemetry.capture_bucket_correction_deadline` = `bucket_start + capture_interval_seconds + late_arrival_tolerance_seconds`); `v_avail = max(telemetry.environment_measurements.bucket_start)` — the **actual newest closed source bucket**, a real persisted timestamp that **cannot run ahead of real data** (contrast the `ca_energy_*` materialization watermark used by migration 209's native tiers), **not** a CAGG watermark, **not** the routing `pipeline_state('environment_measurements')` checkpoint (that is `platform_received_at` / ingest time); `v_start = COALESCE(v_ckpt, clock_timestamp() - v_lookback)` (`lookback` is now only the first-run floor); `v_to = date_bin('1 day', LEAST(LEAST(clock_timestamp() - v_grace, v_avail), v_start + v_max_catchup_window), '2000-01-01 00:00:00+00')` — floored to a **UTC day boundary** (`date_bin` origin at UTC midnight ⇒ deterministic regardless of session `TimeZone`) so the checkpoint always means exactly *"every site-local day whose `local_day_end <= last_received_at` has been finalized"* and **can never jump past a finalizable local-day frontier**. If `v_avail IS NULL` or `v_to <= v_start` → `NO_SOURCE_DATA` (or `SUCCESS` if `v_to = v_ckpt`), **no advance**, `RETURN`. Otherwise `v_from = v_start - v_overlap`; `CALL telemetry.refresh_environment_daily(v_from, v_to)` (unchanged); advance `last_received_at = v_to` as the last write of the run's single transaction. `EXCEPTION WHEN OTHERS → FAILED → RAISE` ⇒ the whole job transaction (advance + every `environment_daily` upsert) rolls back; the retry reprocesses the identical window.
  3. **Job config** gains `max_catchup_window` (`2 days`), `overlap` (`1 day`), `reconcile_window` (`35 days`; stored now, consumed by migration 212 only). `lookback` (`8 days`, first-run floor), `schedule_interval` (`1 hour`), `max_runtime` (`10 minutes`), `max_retries` (`3`), `retry_period` (`5 minutes`), and the migration-208 `check_config` validator (already accepts all four keys since 209) are **unchanged**.
- **Why**: a site-local day first (and only) processed while it lay outside the trailing 8-day window was never revisited, even after late environment source data arrived; and the job re-scanned/re-UPSERTed 8 days of rows every hour (`calculated_at` churn). `environment_daily` is a **write-only durable historian** (no Grafana/view/app/API consumer; `environment_measurements` retention 1 y, `environment_daily` retention none) — a silent gap there is invisible for years and becomes irrecoverable once the source ages out, so a truthful, conservative watermark matters even though nothing reads the table yet.
- **Scope boundary**: `telemetry.refresh_environment_daily` calculation logic, the `environment_daily` schema/PK/indexes, `ca_environment_15min` / `ca_environment_hourly` and their refresh policies, retention, compression, every Grafana object, the routing pipeline (jobs 1001/1012), and job cadence/runtime/retry are all **untouched**. Reconciliation + `pipeline_reconciliation_log` + `v_pipeline_health` are migration 212 — **not started**.
- **Evidence/source**: Local disposable-database integration suite green. New `scripts/test/assert_environment_daily_watermark.sh` (rollback-only; holds the env-daily advisory lock for the whole transaction) covers: `T-DST-fall` (25-hour local day `2025-11-02` America/New_York → exactly one row, correct `observation_date` / `bucket_start` = NY local midnight in UTC / `sample_count`); `T-DST-spring` (23-hour local day `2025-03-09`); `T-partial` (a local day with `local_day_end > p_to` is excluded, then emitted once the window covers its end); `T-late` (a late `environment_measurements` bucket for an already-finalized local day is folded in on a covering re-run — `sample_count` and `temperature_c_avg` move); `T-idem` (unchanged re-run → identical aggregates; `calculated_at` churn is expected and unguarded); `T-WM-first` (checkpoint NULL → advances to `date_bin('1 day', LEAST(LEAST(now-grace, parent), floor+max_catchup))`); `T-WM-catchup` (checkpoint 10 d behind, parent recent → advance bounded to `date_bin('1 day', checkpoint + max_catchup_window)`, not `now - grace`); `T-WM-stalled` (parent not ahead of checkpoint → no advance); `T-WM-frontier` (after a bounded catch-up, every finalized local-day start is strictly below the watermark); `T-WM-fail` (injected failure on the advance write → `RAISE` propagates, checkpoint intact); `T-lock` (wrapper retains the 208 advisory-lock / `SKIPPED_LOCKED` / `FAILED→RAISE` scaffolding, uses `max(environment_measurements.bucket_start)` as parent, and references **no** `ca_environment_*`). `assert_analytics_job_hardening` updated for 211 (env-daily wrapper check now asserts the watermark advance + real-source parent + no-CAGG + UTC-day/capture-policy-grace derivation; the "no watermark" tier list `v_nowm` is now empty — all 7 analytical tiers are watermark-driven). `assert_job_schedule_canonical` unchanged (checks `config.lookback = '8 days'` — preserved) and green. The wrapper is hand-written (small procedure); `telemetry.refresh_environment_daily` needed no generation because it is not modified — minimal delta vs the deployed 208 wrapper: DECLARE block +5 vars +3 config reads +validation; the 3-line "Window derivation" block replaced by the watermark derivation + guard; `v_ckpt` `FOR UPDATE` read added after the lock; terminal `pipeline_state` UPDATE gains `last_received_at = v_to`; advisory-lock / `RUNNING` / `EXCEPTION` blocks verbatim.
- **Verified live**: **Not yet.** Repository + local disposable-database only. Before staging deploy, read the staging max enabled `config.telemetry_capture_policies.late_arrival_tolerance_seconds` / `capture_interval_seconds` (drives `v_grace`) and `environment_daily` freshness / `job_stats`.
- **Related**: `postgres/migrations/211_environment_daily_watermark.sql`, `scripts/test/assert_environment_daily_watermark.sh`, `scripts/test/assert_analytics_job_hardening.sql` (updated), `scripts/test/run_integration_environment.sh`, `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — Migration 211 Pre-Implementation Audit` (in-session).
- **Outcome**: Implemented and locally validated; awaiting review. The bounded trailing reconciliation work (`pipeline_reconciliation_log` + completeness-aware `reconcile_*` procedures + `v_pipeline_health`) is now **migration 213 / 214** (renumbered — see the migration-212 entry below); it has **not** been started and is the safety net for the migration-209 `ca_energy_*`-materialization-watermark-overshoot condition (a CAGG refresh can advance across empty source periods, so an energy child checkpoint can advance over a window that later backfills below it).

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 0 (completion) / N1: bounded normalization forward window for job 1000 (migration 212)

- **Area**: Telemetry pipeline watermarks / bounded catch-up ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8)
- **What changed**: Migration 212 wires job 1000's scheduled wrapper `telemetry.run_normalization_job` to the bounded-window mechanism that migration 205 already added to the loader. The wrapper (`CREATE OR REPLACE`, arity unchanged, no `DROP`) now derives `v_max_window` from `config.max_window` (default `INTERVAL '2 hours'`, positive-only) and **always** passes it as the second argument: `CALL telemetry.load_normalized_points_incremental(v_overlap, v_max_window)` — it never passes `NULL`. Job 1000's live config gains `"max_window": "2 hours"` alongside the existing `"overlap": "15 minutes"` via a guarded idempotent `alter_job` (mirrors migration 207 §4 exactly). This is the migration-207 wrapper pattern applied to job 1000.
- **Why**: `telemetry.run_normalization_job` still called the loader with **one argument** (`p_max_window` → `DEFAULT NULL`), so the loader computed its forward endpoint as `max(telemetry.raw_messages.received_at)` with no cap and processed the entire forward window in **one transaction**. Once the checkpoint-to-now gap exceeded the job's 5-minute `max_runtime`, every retry faced an equal-or-wider window and could make zero durable progress — the exact 2026-08-26 job-1000 stall class, and the last remaining instance of it after migration 207 fixed the routing jobs. Migration 205 gave this loader a *manual* bounded catch-up capability; migration 212 makes the *scheduled* path self-draining: an unattended multi-hour `raw_messages` backlog now drains over successive 1-minute runs, each of which durably commits.
- **Scope boundary**: `telemetry.load_normalized_points_incremental` is **not** touched — its advisory lock, `telemetry.pipeline_state` `RUNNING`/`SUCCESS`/`NO_SOURCE_DATA`/`SKIPPED_LOCKED`/`FAILED` handling, single-transaction boundary, `EXCEPTION → FAILED → RAISE` rollback, dynamic overlap, `p_max_window` validation, and 48h bounded first run all remain exactly where they already live. **Not** changed: normalization selection/calculation semantics; `telemetry.normalized_points` / `raw_messages` / `capture_bucket_samples`; job 1000's `schedule_interval` (1 minute), `max_runtime` (5 minutes), `max_retries` (3), `retry_period` (1 minute), `scheduled`; `postgres/jobs/42_normalization_background_job.sql` (canonical file — the migration-207 precedent of not touching `jobs/*.sql` when a migration redefines a wrapper); **`postgres/maintenance/45_rebuild_normalized_history.sql`** and every bare / explicit-`NULL` loader `CALL` (unrestricted forward catch-up remains available for a deliberate operator rebuild); routing jobs, energy consumption, demand, `environment_daily`, any CAGG or its policies, Grafana, application/API code; ownership / `SECURITY` / `search_path` / ACL of any object. No new config validator (job 1000 has no `check_config`; the wrapper validates `max_window` itself, as migration 207 does for the routing wrappers).
- **`max_window` value**: **2 hours, provisional.** No staging job-1000 runtime evidence was available (SSH-only, not contacted). The value is overridable via `alter_job` with no code change; the remedy for a demonstrably-too-large default is an `alter_job` to `1 hour`, not a migration. **Staging runtime validation remains outstanding** (`job_stats` / `job_history` per-run duration distribution for job 1000).
- **Evidence/source**: Local disposable-database integration suite green from a clean DB through migration 212; `EXIT=0`. `scripts/test/assert_normalization_bounded_catchup_window.sql` extended (now covers migrations 205 **and** 212): TESTS A–D (migration 205, loader) unchanged; **TEST E rewritten** — `run_normalization_job` with no `max_window` key now bounds the watermark to `checkpoint + 2h` (was: asserts an unbounded advance) — the reason is documented in the file header; **TESTS F–L added** — config override accepted (`+30 min`), zero/negative `max_window` rejected before the loader with the checkpoint untouched, successive default runs advance `+2h` each, a `<2h` backlog still reaches `max(received_at)`, the deployed wrapper passes a two-argument call and reads `config->>'max_window'` and does **not** duplicate loader-owned lock/state/exception handling while the loader still owns all of it, job 1000 config carries a positive `max_window` alongside `overlap`, and no `normalized_points` rows are produced by the boundary-only fixture (selection/calculation untouched). `assert_job_schedule_canonical` unchanged and green (its contract is `config.overlap` — adding `config.max_window` does not affect it, exactly as migration 207 established for jobs 1001/1012).
- **Verified live**: **Not yet.** Repository + local disposable-database only. Not deployed to staging, not committed, not pushed.
- **Related**: `postgres/migrations/212_normalization_bounded_window.sql`, `scripts/test/assert_normalization_bounded_catchup_window.sql` (extended), `postgres/restructure_manifest.csv`; design of record `Audit/EMS Analytics Platform — N1 + Reconciliation Architecture Redesign Audit v2 (STAGING, design only).txt` and the migration-212 Phase 0 pre-implementation audit (in-session).
- **Outcome**: Implemented and locally validated; awaiting review. **Migration 213** (completeness-aware reconciliation + `analytics.pipeline_reconciliation_log`) and **Migration 214** (`analytics.v_pipeline_health`) were **NOT** implemented and each requires separate explicit approval.

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 1 (4/4): analytical reconciliation layer (migration 213)

- **Area**: Analytics / aggregation reconciliation ([11-aggregation.md](11-aggregation.md), [10-analytics.md](10-analytics.md)), telemetry pipeline watermarks ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8)
- **What changed**: Migration 213 adds a bounded, observable, idempotent **trailing re-drive** for the seven analytical tiers — a corrective safety net that repairs sub-checkpoint holes the forward watermark cannot see (late source backfilled *below* the checkpoint; `ca_energy_1min`/`ca_energy_5min` materialisation-watermark overshoot; a bad manual/effective-dated write to an already-finalised row) **without ever advancing a forward checkpoint**. New objects:
  1. **`analytics.pipeline_reconciliation_log`** — hypertable on `ran_at` (30-day chunks), PK `(run_id, ran_at)`, one row per reconcile run (no bucket-level detail, no tenant columns), a `(tier, ran_at DESC)` index and a partial `(tier, ran_at DESC) WHERE outcome IN ('FAILED','PARTIAL') OR error_count > 0` attention index, 180-day `add_retention_policy`, owner `ems_admin`, `SELECT` to `ems_readonly` + `grafana_reader`. `outcome ∈ {HEALTHY, REPAIRED, PARTIAL, FAILED, SKIPPED_LOCKED, NO_CHECKPOINT}`; `window_start`/`window_end` record the exact inspected interval.
  2. **`config.assert_reconciliation_job_config(jsonb)`** — `check_config` validator wired onto all seven jobs; accepts only `{reconcile_window, coarse, n_max, lookback}`, rejects a non-object, an unknown key, a non-positive interval, or a non-positive `n_max` (mirrors `config.assert_analytical_lookback_job_config`).
  3. **`analytics.record_reconciliation_run(...)`** — shared log-writer (`SECURITY DEFINER`, pinned `search_path`), fills `duration_ms` from the run start; contains no reconciliation logic.
  4. **`analytics.reconcile_energy_deficits(p_tier, p_window_start, p_window_end, p_coarse, p_limit)`** — one **read-only** `STABLE` per-tier deficit detector for `energy_consumption_1min`/`5min`/`15min`/`hourly`. Returns the oldest `p_limit` coarse buckets that contain at least one `(device, tier-bucket)` whose persisted fingerprint disagrees with the current parent. No writes, no `refresh_*`, no `refresh_continuous_aggregate`, no `pipeline_state` access. Fingerprints: `1min`/`5min` → `child.source_sample_count` vs `telemetry.ca_energy_{1,5}min.sample_count` per `(device, minute)`, honouring the calc function's `resolve_site_capture_bucket` capture-interval eligibility filter (`<= 60` / `= 300`); `15min` → `count(energy_consumption_1min UNION ALL energy_consumption_5min` rows per `(device, IST-origin 15-min bucket))` vs `source_interval_count`, **plus** `max(native.calculated_at) > child.calculated_at`; `hourly` → `sum(energy_consumption_15min.source_interval_count` per `(device, hour))` vs `source_interval_count`, plus `calculated_at` recency. "Child present, parent absent" (source retracted) is **not** flagged for repair.
  5. **Seven tier-specific reconcile procedures** (`analytics.reconcile_energy_consumption_{1min,5min,15min,hourly,daily}`, `analytics.reconcile_demand_intervals`, `telemetry.reconcile_environment_daily`) — each `SECURITY DEFINER`, pinned `search_path`, owner `ems_admin`, `REVOKE ALL … FROM PUBLIC`. Each takes **the same transaction-scoped advisory key as its forward job** (`pg_try_advisory_xact_lock(hashtextextended('<forward run_*_job proc>', 0))`) → a reconcile and its forward job (or two reconcile runs of the same tier) can never overlap; the loser records `SKIPPED_LOCKED`. Each reads `telemetry.pipeline_state(tier).last_received_at` **only to derive `window_start = checkpoint − reconcile_window`** (never writes it); a `NULL` checkpoint records `NO_CHECKPOINT` and returns. `1min`/`5min`/`15min`/`hourly` iterate the detector's coarse buckets and call the **unchanged** `analytics.refresh_energy_consumption_<tier>(bucket, bucket + coarse)` per bucket, bounded by `n_max` (ships **6**; exceeding it records `PARTIAL` and leaves the remainder for the next run); `daily` uses an **inline site-local-day** detector (`metadata.sites.timezone`, DST-aware) and calls `analytics.refresh_energy_consumption_daily(local_start_utc, local_end_utc)`; `demand` has no source-count fingerprint — the repair *is* the re-run: `CALL analytics.refresh_demand_analytics(clock_timestamp(), lookback, p_finalize_from => checkpoint − reconcile_window, p_finalize_to => checkpoint)` (the migration-210 status guard freezes `VALID` rows and branches the SITE/ASSET partial-index upsert; the live `demand_state` block stays keyed to `p_now`); `environment_daily` uses an inline site-local-day detector (a `(device, local-day)` whose `environment_measurements` bucket count now exceeds `environment_daily.sample_count`, or has source but no row) and calls the unchanged `telemetry.refresh_environment_daily(local_start_utc, local_end_utc)` (ships `n_max` **8**). Each individual `refresh_*` call is wrapped in a per-coarse-bucket / per-scope `BEGIN … EXCEPTION` subtransaction: one unit's failure stops the loop and is logged (`FAILED`, `first_error_sqlstate`/`first_error_message` set), but earlier successful re-drives in the same run persist; the procedure is otherwise a single transaction with no `COMMIT`/`ROLLBACK`, so on `max_runtime` cancellation nothing is repaired and nothing advanced.
  6. **Seven reconcile jobs** (`add_job`, `fixed_schedule`, guarded by proc-name lookup so re-application is a no-op): `reconcile_energy_consumption_1min`/`_5min` + `reconcile_demand_intervals` every **1 h**; `_15min` every **6 h**; `_hourly` every **12 h**; `_daily` + `reconcile_environment_daily` every **24 h**; per-tier phase offsets (29–51 min past the anchor hour) so a reconcile does not perpetually lose the advisory-lock race to a forward run; `max_runtime` 5 min (fast tiers) / 10 min (slow), `max_retries` 3, `retry_period` 15/30 min; config carries the per-tier `reconcile_window` (`2 days` / `7 days` / `8 days` / `10 days` / `21 days` / `6 hours` / `35 days`), `coarse`, and `n_max`.
  7. **One forward-job config change (N2)**: `analytics.run_energy_consumption_15min_job` `config.reconcile_window` **3 days → 8 days** (the 15-min tier consumes `energy_consumption_1min UNION ALL energy_consumption_5min`; a `ca_energy_5min`-fed 5-min repair can be up to that CAGG's 7-day `start_offset` old, so a 3-day 15-min reconcile window could fail to propagate it). The forward job's window derivation, schedule, runtime, retries, `check_config`, `max_catchup_window`, and every other config key are unchanged.
- **Why**: Migration 213 Pre-Implementation Reconciliation Audit + N1/Reconciliation Architecture Redesign Audit v2. The forward pipeline (209/210/211) advances a high-water mark only over a contiguous bounded slice; a high-water mark structurally cannot see (G1) late source corrected/backfilled below the checkpoint, (G2) `ca_energy_*` materialisation-watermark overshoot — the CAGG refresh policy advances the scalar watermark across empty source periods, so an energy child can advance its checkpoint over a range that has no CAGG rows and never re-reads it once source arrives, or (G3) a retroactive effective-dated config change / bad manual write to already-finalised rows. The trailing re-drive closes G1–G3 within a bounded window, on a slow cadence, with every run's outcome persisted for an operator to read.
- **CAGG-refresh prohibition (hard invariant)**: **No** automated reconcile object calls `public.refresh_continuous_aggregate` — the v2 audit's local probe proved TimescaleDB 2.29.2 rejects it inside any transaction or `BEGIN … EXCEPTION` subtransaction. `reconcile_window` for `1min` = **2 days** and for `5min` = **7 days**, i.e. exactly each CAGG's automatic-refresh `start_offset`, so the automatic policy keeps the CAGG truthful *within* the reconcile window. Backfill **older** than the `start_offset` is a documented **operator-remediation** condition (top-level `CALL public.refresh_continuous_aggregate(...)`, then the next reconcile pass repairs the children) — it is neither auto-repaired nor silently reported `HEALTHY`. Migration 214 (`analytics.v_pipeline_health`, **not** part of this migration) will surface it.
- **Watermark isolation (hard invariant, automated contract test)**: an automated check greps `pg_get_functiondef` of every new reconcile object — none assigns `telemetry.pipeline_state.last_received_at`, runs DML against `telemetry.pipeline_state`, calls a `run_*_job` forward wrapper, or calls `refresh_continuous_aggregate`; and the complete set of `last_received_at` writers is still exactly the seven forward wrappers + five telemetry loaders (12), unchanged by this migration. Every behavioural test additionally samples `pipeline_state.last_received_at` before and after each reconcile `CALL` and fails on any change, including on `REPAIRED`, `PARTIAL`, and `FAILED` runs.
- **Deviation from the approved audit**: **1, factual, no architecture change.** The 213 audit (§5/§7/§10) stated `analytics.energy_consumption_15min` persists a `source_sample_count` column that the 15-min detector compares. Re-verified against the deployed schema: `energy_consumption_15min` (and `_hourly`, `_daily`) has **`source_interval_count`** only — **no `source_sample_count`** (the audit conflated the `v_energy_semantic_rollup_15min` *view's* output column, which is named `source_sample_count`, with the persisted *table's* columns). `energy_consumption_1min`/`_5min` do have `source_sample_count`. Resolution ("current state wins", not a v1/v2 contradiction): the 15-min / hourly / daily detectors use a count/`SUM` fingerprint mismatch on `source_interval_count` **plus** a recompute-recency check `max(parent.calculated_at) > child.calculated_at` (all five tiers carry `calculated_at`; `ca_energy_1min` does not, which is why the `1min`/`5min` tiers keep the `source_sample_count` fingerprint). Every hard invariant (no `pipeline_state` write, no CAGG refresh, advisory-key sharing, `n_max` bounding, per-unit isolation, Option-C shape) is unaffected.
- **Scope boundary**: **Not** touched — any `refresh_*` calculation body; any `run_*_job` forward wrapper; `telemetry.pipeline_state` schema or rows; any CAGG or CAGG policy; any retention/compression policy on an existing table; the schedule / runtime / retries of any forward job; demand `max_catchup_window` (stays 6 h); migration 212; `postgres/jobs/*.sql`; Grafana; application/API code; normalization; routing; calculation/quality semantics. The demand reconcile window is **6 hours** — the audit's conditional 12 h recommendation depends on a staging cost read (O-D) that was **not** performed, so the safe approved value is used and raising it later is an `alter_job`, not a migration. **Migration 214 (`analytics.v_pipeline_health`) is not implemented here** and requires separate explicit approval.
- **Evidence/source**: Local disposable-database integration suite green from a **clean DB** through migration 213; `EXIT=0` (re-run to confirm). Migration 213 applies via the canonical harness with ledger row + `sha256` checksum `4661c9f3…b40b4f95`; migrations 205–212 remain green; ledger reaches 213. New `scripts/test/assert_analytical_reconciliation.sh` (rollback-only main heredoc holding all seven forward advisory locks + a cross-session `SKIPPED_LOCKED` sub-test on the `assert_analytics_job_self_overlap.sh` background-psql pattern) passes: **A** schema/hypertable/PK/2 indexes/180-day retention/read grants; **B** validator accept/reject; **C** all 7 procs `DEFINER`/`ems_admin`/pinned/`REVOKE` + body contracts (no `last_received_at` write, no CAGG refresh, no wrapper call, correct shared advisory key, correct `refresh_*` call, `record_reconciliation_run`), helper read-only; **D** `last_received_at` writer set unchanged (12); **E** 7 jobs registered once each with the expected schedule/runtime/retry/`check_config`/config; **F** N2 applied (15-min `reconcile_window` 8 d), forward-job timing / demand `max_catchup_window` unchanged; **G** every proc: `NULL` checkpoint → `NO_CHECKPOINT`, no `pipeline_state` movement; **H/X** hourly `MISSING_CHILD` repaired via `refresh_energy_consumption_hourly` (`source_interval_count` = `SUM`), inspected window = `[cp − reconcile_window, cp)`, forward checkpoint intact, a correct row outside the repaired coarse bucket untouched; **I** idempotent (2nd run `HEALTHY`/0); **K** `n_max` bounds one run (3 mismatching hours, `n_max` 2 → `PARTIAL`/2 repaired; draining runs → `HEALTHY`); **L** per-unit failure isolation (one hour's repair raises → loop stops, `FAILED` + `first_error_sqlstate` logged, earlier re-drive persists, checkpoint intact); **M** 15-min detector (native-without-child flagged; matching child clears; child staler than native re-flagged); **N** 1-min detector (`ca_energy_1min` bucket without child flagged; matching `source_sample_count` clears; disagreeing count re-flagged; capture-eligibility filter honoured); **P** daily site-local-day `MISSING_CHILD` repaired via `refresh_energy_consumption_daily`, checkpoint intact, idempotent; **T** `environment_daily` `LATE_SOURCE` re-driven via `refresh_environment_daily` (site-local bounds), `pipeline_state` intact, strict signal idempotent; **S** demand bounded 4-arg `refresh_demand_analytics` path, inspected window = `[cp − reconcile_window, cp)`, `pipeline_state` intact (full `NO_DATA` → `VALID` re-finalisation is exercised by the migration-210 fixture in `assert_demand_watermark_refinalization.sh`); **J (cross-session)** a reconcile records `SKIPPED_LOCKED` with `NULL` window when a background session holds the forward advisory lock. `assert_analytical_reconciliation.sh` added to `run_integration_environment.sh` before the ledger step.
- **Verified live**: **Not yet.** Repository + local disposable-database only. Not deployed to staging, not committed, not pushed. Before any staging deploy, sample per-tier single-window reconcile cost (`job_stats` / a manual bounded `reconcile_*` `CALL`) — particularly `energy_consumption_15min` (8-day window) and `environment_daily` (35-day window) — and decide the demand `reconcile_window` (O-D: retain 6 h unless a measured 12 h catch-up cost is acceptable).
- **Related**: `postgres/migrations/213_analytical_reconciliation.sql`, `scripts/test/assert_analytical_reconciliation.sh`, `scripts/test/run_integration_environment.sh` (assert registered), `postgres/restructure_manifest.csv`; designs of record `Audit/EMS Analytics Platform — Migration 213 Pre-Implementation Reconciliation Audit.txt` and `Audit/EMS Analytics Platform — N1 + Reconciliation Architecture Redesign Audit v2 (STAGING, design only).txt`.
- **Outcome**: Implemented and locally validated; awaiting review. **Migration 214** (`analytics.v_pipeline_health` — the read-only health view over `pipeline_state` + `pipeline_reconciliation_log`, including the CAGG-older-than-`start_offset` operator-remediation signal) was **NOT** implemented and requires separate explicit approval. This completes Phase 2 Foundation Phase 1 (4/4).

### 2026-08-27 — EMS Analytics Platform Phase 2 Foundation, Phase 1 companion: operator pipeline-health view (migration 214)

- **Area**: Analytics observability ([10-analytics.md](10-analytics.md), [11-aggregation.md](11-aggregation.md)), telemetry pipeline watermarks ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), operations/diagnostics ([19-operations-and-diagnostics.md](19-operations-and-diagnostics.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) §8)
- **What changed**: Migration 214 adds one **read-only, system/operator-scoped view** `analytics.v_pipeline_health` — **one row per reconciliation domain** (the seven 213 tiers). It *exposes* the failure modes that motivated migrations 209–213; it *repairs nothing*. It is a plain view (owner `ems_admin`, **not** `security_invoker`, so underlying-table access uses the owner's privileges), `REVOKE ALL … FROM PUBLIC`, `GRANT SELECT … TO grafana_reader`. No new table, no new index, no new job, no config-table dependency. **Four explicit health dimensions**, never collapsed to one opaque field:
  1. **`forward_state`** — from `telemetry.pipeline_state(tier)` + the forward job's own metadata: `NOT_INITIALIZED` (`last_received_at IS NULL`) → `FAILED` (`last_status='FAILED'`) → `RUNNING_STALE` (`RUNNING` for `> 2 × max_runtime`) → `STALE` (checkpoint age `> 4 × schedule_interval + max_catchup_window`) → `NO_SOURCE_DATA` → `RUNNING` → `OK`.
  2. **`reconcile_state`** — from `analytics.pipeline_reconciliation_log` (latest run per `tier` via the 213 `ix_pipeline_reconciliation_log_tier_time`, plus a bounded last-10-rows window): `NOT_INITIALIZED` (no row, or latest `outcome='NO_CHECKPOINT'`) → `FAILED` (latest `outcome='FAILED'`; `first_error_sqlstate` surfaced, **the raw message is not**) → `STALE` (last run age `> 3 × reconcile schedule_interval`, or the job is `Paused`) → `BACKLOGGED` (latest **and** previous run both `PARTIAL`) → `CONTENDED` (last 3 runs all `SKIPPED_LOCKED`) → `PARTIAL` (single) → `SKIPPED_LOCKED` (single) → `OK` (`HEALTHY`/`REPAIRED`).
  3. **`integrity_risk`** — `N/A` for `demand_intervals` and `environment_daily` (no CAGG, no count fingerprint); `PERSISTENT_DEFICIT` (energy tiers) when the last 3 reconcile runs all repaired `rows_repaired > 0`; **`CAGG_OVERSHOOT`** for `energy_consumption_1min`/`_5min` when the `ca_energy_{1,5}min` materialisation watermark runs ahead of `max(telemetry.energy_measurements.bucket_start)` by more than the derived safety margin (below); otherwise **`OLDER_BACKFILL_UNKNOWN`** for the two native tiers (see C2).
  4. **`health`** — `ERROR` (forward `FAILED`/`RUNNING_STALE`, reconcile `FAILED`/`STALE`, `PERSISTENT_DEFICIT`) `>` `WARNING` (forward `STALE`, reconcile `BACKLOGGED`/`CONTENDED`, `CAGG_OVERSHOOT`) `>` `UNKNOWN` (either axis `NOT_INITIALIZED`, or `OLDER_BACKFILL_UNKNOWN` with nothing worse) `>` `OK`. Plus `health_rank smallint` (3/2/1/0) for sorting and `health_reason text`.
- **Thresholds (C4)**: derived from each job's own `schedule_interval` / `max_runtime` / `config->>'max_catchup_window'` (read from `timescaledb_information.jobs` as the view owner), with three documented missed-cadence multipliers `k_fwd = 4`, `k_run = 2`, `k_rec = 3`. `BACKLOGGED`/`CONTENDED`/`PERSISTENT_DEFICIT` use consecutive-log-row evidence, no age threshold. **No `config.pipeline_health_policy` table** — schedule-derived semantics proved sufficient.
- **CAGG overshoot margin (re-derived from current CAGG semantics)**: `ca_energy_1min`/`_5min` are `materialized_only` with a **1-minute `end_offset`**, so the watermark legitimately trails "now" *and* legitimately runs ahead of the newest *stable* source bucket by up to one late-arrival correction window. The naive `watermark − max(source) > 0` test would fire constantly. `overshoot_margin = MAX(capture_interval_seconds + late_arrival_tolerance_seconds)` over enabled `config.telemetry_capture_policies` `+ CAGG end_offset` (from the refresh-policy config) `+ 2 × run_energy_routing_job schedule_interval` `+ 120 s`. With current data ≈ **21 minutes**. `CAGG_OVERSHOOT` is a **WARNING** — the 213 reconcile self-heals it within one reconcile cycle once source lands.
- **C2 — older-than-`start_offset` backfill is NOT reliably detectable in a live view**: a local read-only probe of `_timescaledb_catalog.continuous_aggs_materialization_invalidation_log` found it unsuitable as a canonical signal on TimescaleDB 2.29.2 (bootstrap sentinel rows for every CAGG even on an empty database; internal-integer time; `±infinity` sentinels; entries merged/consumed on every refresh; no compatibility guarantee). The view therefore **does not read it**. The condition is surfaced as `integrity_risk = 'OLDER_BACKFILL_UNKNOWN'` for the two native tiers (whenever no higher-precedence risk applies) + `older_backfill_horizon` (= the latest reconcile `window_start`, the point below which automated repair provably cannot see) + an **on-demand operator diagnostic** in [19-operations-and-diagnostics.md](19-operations-and-diagnostics.md).
- **C3 — query cost**: `EXPLAIN (ANALYZE, BUFFERS)` on the local DB with representative data → **~6–7 ms**, all `shared hit`. Source frontiers are per-chunk `Index (Only) Scan Backward` on the existing `*_bucket_start_idx` (newest chunk); latest-per-tier uses the 213 `(tier, ran_at DESC)` index; `pipeline_state` is a 12-row scan; `timescaledb_information.jobs`/`job_stats` are ~85-row scans. **No `Seq Scan`** of `energy_measurements` / `environment_measurements` / `energy_consumption_*` / the reconcile-log chunks. The `assessed` CTE is `MATERIALIZED` so each axis `CASE` (and the inline watermark read) is evaluated once for the seven rows.
- **Deviations from the approved 214 audit (current-state-wins, §33 rule)**:
  - **D1** — the audit (§24) planned to reuse `analytics.cagg_available_through(regclass)`. Verified: that function is `SECURITY INVOKER` and its internal `format(…)::regclass` cast requires `USAGE ON SCHEMA telemetry`, which `grafana_reader` **lacks** ("permission denied for schema telemetry"). 214 instead reads the watermark **inline** from `_timescaledb_catalog.continuous_agg` + `continuous_aggs_watermark` **by schema/name** (no `::regclass`), converting via `_timescaledb_functions.to_timestamp` and `NULLIF`-ing the `4714-…BC` sentinel — the same logic, evaluated with the view owner's privileges. `analytics.cagg_available_through` is **not modified** (209 untouched).
  - **D2** — the audit (§21/§24/§30) planned `GRANT … TO ems_readonly, grafana_reader` (mirroring the 213 `pipeline_reconciliation_log` grant). Verified: `ems_readonly` has **no `USAGE ON SCHEMA analytics`** and cannot query *any* `analytics.v_*` view; every existing `analytics.v_*` is granted to `grafana_reader` only. 214 grants to **`grafana_reader` only** (strictly more restrictive; no tenant-isolation mechanism changed). The pre-existing inert 213 `ems_readonly` grant on `pipeline_reconciliation_log` is left as-is.
- **Scope boundary**: **Not** touched — migrations 209–213; any `refresh_*` calculation body; any `run_*_job` forward wrapper; any `reconcile_*` procedure or reconcile job; the forward jobs; `telemetry.pipeline_state`; `analytics.pipeline_reconciliation_log`; `analytics.cagg_available_through`; any CAGG or CAGG policy; retention/compression; routing; normalization; Grafana; application/API code; tenant-isolation mechanisms. No tenant columns are added (pipeline machinery is platform-global — a stalled job affects every tenant identically). This is not a `v_grafana_*` view and carries no `grafana_org_id`; it must not be wired into a tenant-facing dashboard. **214 ships no Grafana dashboard JSON** — an operator "Pipeline Health" dashboard, if wanted, is a separate follow-up. A system-level roll-up view is explicitly out of scope (derivable by `MAX(health_rank)`).
- **Evidence/source**: Local disposable-database integration suite green from a **clean DB** through migration 214; `EXIT=0` (run twice). Migration 214 applies via the canonical harness with ledger row + `sha256` checksum `4e80eded2dda42db5e94e7f738b13ea6329d71698dc05c60f5f015d7cdbb0872`; migrations 205–213 remain green; ledger reaches 214. New `scripts/test/assert_pipeline_health_view.sh` (rollback-only, 19 assertions **A–S**): **A** view exists / plain view / 7 rows (one per domain); **B** exact 37-column contract + key types + raw error text **not** a column; **C** owner `ems_admin` / not `security_invoker` / `SELECT` to `grafana_reader` only / revoked from `PUBLIC` / no write privs; **D** view body calls no repair / CAGG-refresh / advisory-lock / sequence function and depends on no `VOLATILE` function; **E** `SELECT`ing the view mutates neither `pipeline_state` nor `pipeline_reconciliation_log`; **F** all-OK HEALTHY; **G** `FAILED` reconcile → `health=ERROR`, SQLSTATE surfaced; **H** single `PARTIAL` → `OK`, two consecutive → `BACKLOGGED`/`WARNING`; **I** single `SKIPPED_LOCKED` → `OK`, three → `CONTENDED`/`WARNING`; **J** NULL checkpoint + `NO_CHECKPOINT` → both `NOT_INITIALIZED`, `health=UNKNOWN`; **K** `NO_SOURCE_DATA` forward with a fresh checkpoint → `OK`; **L** forward checkpoint older than `k_fwd × schedule + max_catchup_window` → `STALE`/`WARNING`; **M** reconcile last run older than `k_rec × reconcile schedule` → `STALE`/`ERROR`; **N** three consecutive `rows_repaired > 0` → `PERSISTENT_DEFICIT`/`ERROR`; **O** watermark far ahead of source → `CAGG_OVERSHOOT`/`WARNING`, within margin → `OLDER_BACKFILL_UNKNOWN`; **P** native-tier older-backfill limitation surfaced (`OLDER_BACKFILL_UNKNOWN` + `older_backfill_horizon` + diagnostic pointer); **Q** no tenant/site/device column, `grafana_reader` reads all 7 rows; **R** `EXPLAIN` shows no `Seq Scan` of the large source/analytical/reconcile-log tables, latest-per-tier uses the 213 index, execution < 250 ms (~7 ms observed); **S** fresh/disposable DB → 7 rows, all `NOT_INITIALIZED`/`UNKNOWN`, no error. `assert_pipeline_health_view.sh` added to `run_integration_environment.sh` before the ledger step; no existing test weakened.
- **Verified live**: **Not yet.** Repository + local disposable-database only. Not deployed to staging, not committed, not pushed. **Outstanding staging read (C1, non-blocking)** — before any staging deploy, sample the current CAGG-overshoot magnitude and enabled capture policies to confirm the healthy `overshoot` distribution sits below the ~21-minute margin, and confirm the `max(energy_measurements.bucket_start)` frontier `EXPLAIN` at multi-hundred-chunk scale. Exact read-only queries are in the migration-214 implementation report; they were **not** executed.
- **Related**: `postgres/migrations/214_pipeline_health_view.sql`, `scripts/test/assert_pipeline_health_view.sh`, `scripts/test/run_integration_environment.sh` (assert registered), `postgres/restructure_manifest.csv`, `docs/platform-manual/19-operations-and-diagnostics.md` (older-backfill diagnostic); design of record `Audit/EMS Analytics Platform — Migration 214 Pre-Implementation Audit` (in-session).
- **Outcome**: Implemented and locally validated; awaiting review. Phase 2 Foundation Phase 1 is now complete (209–213) with its operator health surface (214).

### 2026-08-29 — Migration 219: Best Energy "Air Sense" made compatible with the existing `ENVIRONMENT_SENSOR_AIRSENSE_V1` telemetry profile

- **Area**: Device onboarding ([08-device-onboarding.md](08-device-onboarding.md)), admin portal ([13-admin-portal.md](13-admin-portal.md)), reference/config data ([06-data-model.md](06-data-model.md)), known issues ([23-known-issues-and-drift.md](23-known-issues-and-drift.md) item 7)
- **What changed**: New forward migration `postgres/migrations/219_airsense_environmental_sensor_compatibility.sql` (+ manifest row, + `scripts/test/assert_airsense_environmental_sensor_compatibility.sh`/`.sql` registered in `scripts/test/run_integration_environment.sh` before the ledger step). It inserts **two** idempotent canonical reference rows, resolving every FK by natural key:
  1. `config.device_profile_categories` — `ENVIRONMENT_SENSOR_AIRSENSE_V1` (by `profile_code`) ↔ `Environmental Sensor` (by `config.device_categories.name`). `ON CONFLICT (profile_id, device_category_id) DO NOTHING`.
  2. `metadata.device_models` — `vendor='Best Energy'`, `model='Air Sense'`, `device_type='Environmental Sensor'` (the migration-193 legacy-column contract: `device_type` mirrors the category name), `device_category_id` → `Environmental Sensor`. Guarded on the case-insensitive `device_models_vendor_model_ci_uq` `(lower(coalesce(vendor,'')), lower(model))` shape; an out-of-band row is realigned in place, never duplicated.
  The migration carries its own postconditions (fail-and-rollback if the compatibility row, the model row, or `admin.v_active_device_profiles`' category exposure is not reached), mirroring the migration 198/199 "fail loudly on partial reference data" discipline.
- **Root cause** (staging read-only investigation via `ssh ems-staging`, no mutation): the profile `ENVIRONMENT_SENSOR_AIRSENSE_V1` **already existed and was complete** on staging — `config.device_profiles` row `is_active`, `manufacturer 'Generic'` / `model 'Environment Sensor'`, **12** optional `config.profile_field_mapping` rows (`T1, RH, LL, PIR, PIR_t, dis1, ain1..ain4, Vbat, Stat`). It was **not** missing. What was missing was reference data *about* it: (a) `config.device_profile_categories` held exactly one row (`ENERGY_METER_ENISCOPE_V1 ↔ Energy Meter`, added out of band 2026-08-24) and **none** for the Air Sense profile, so `admin.v_active_device_profiles` returned it with `device_category_ids = {}` and the onboarding UI (`app/src/static/js/onboarding-device.js` → `filterProfiles()`) hid it for every category, and `admin.create_device()` would raise *"Device profile is not compatible with the selected category"*; (b) `metadata.device_models` had no Best Energy / Air Sense row (only `Best Energy / Eniscope Energy Meter`), so the model selector was empty for Environmental Sensor and `admin.create_device()` would raise *"Device model does not match the selected category"*. No repository seed populates `config.device_profile_categories` at all — the standing gap recorded as item 7.
- **Explicitly NOT changed**: the `config.device_profiles` row for `ENVIRONMENT_SENSOR_AIRSENSE_V1` (every column preserved); its 12 `config.profile_field_mapping` rows (no logical-point / normalization semantics touched); `config.device_profile_required_fields` (deliberately still no row for this profile, so `admin.commission_device()`'s migration-199 completeness gate stays a vacuous no-op for Air Sense devices — its environment fields are all optional by design); any application code, view, trigger, or function. The onboarding view, the JS category filter, and the `admin.create_device` compatibility gates were already correct and generic — they simply had no reference row to act on, so **no speculative code change was made**.
- **Evidence/source**: Read-only staging queries (migration ledger at 218; `config.device_profiles`, `config.device_profile_categories`, `config.device_categories`, `metadata.device_models`, `admin.v_active_device_profiles`, `admin.v_device_models`, `config.profile_field_mapping` for the profile, `config.protocols`, `metadata.logical_points`). Local disposable-database integration suite (`scripts/test/run_integration_environment.sh`) run from a clean DB through migration 219 — see the migration-219 verification note appended once the run completes. `admin.schema_migrations` reaches 219 with a `sha256` ledger row.
- **Verified live**: **Staging — pending.** Repository + local disposable-database only at time of writing. Not yet deployed to staging (staging deploy is by the normal CI/CD `staging`-branch path, which applies the manifest via `scripts/apply_migrations.sh`). **Production — not touched**, promotion prepared only.
- **Related**: `postgres/migrations/219_airsense_environmental_sensor_compatibility.sql`, `postgres/restructure_manifest.csv`, `scripts/test/assert_airsense_environmental_sensor_compatibility.sh` / `.sql`, `scripts/test/run_integration_environment.sh`; `postgres/seeds/reference/65_environment_sensor_profile.sql` (the profile), `postgres/seeds/reference/05_lookup_tables.sql` (the category), `postgres/ddl/76_admin_onboarding_contract.sql` (`admin.v_active_device_profiles`), `postgres/ddl/118_device_tab_system_generated_external_id.sql` (`admin.create_device` gates), `app/src/static/js/onboarding-device.js` (`filterProfiles()`); [23-known-issues-and-drift.md](23-known-issues-and-drift.md) item 7.
- **Outcome**: Implemented and locally validated; staging deploy and production promotion await review/approval.

### 2026-08-29 — Migration 220: recovery supersession tests same-bucket membership by interval containment, dropping the per-competing-element `resolve_site_capture_bucket()` fan-out

- **Area**: Telemetry pipeline — failed-message recovery / Job 1077 ([07-telemetry-pipeline.md](07-telemetry-pipeline.md)), performance ([22-performance.md](22-performance.md))
- **What changed**: New forward migration `postgres/migrations/220_recovery_supersession_interval_predicate.sql` (+ manifest row, after 219 and before 222). It is the **migration-218 `telemetry.recover_failed_raw_messages(integer)` body verbatim** — DEFERRED_UNCOMMISSIONED branch and loop-`SELECT` value set (218), retention branch first (206), receipt-time window + rtdata-array guard + device-identity IN-list (204), per-candidate `COMMIT` / bare-top-level `CALL` (203), `normalized_points_for_recovery_candidate` (202), the `ROW(...) > ROW(...)` recency tie-break, `p_limit` range, `ORDER BY raw_received_at`, `FOR UPDATE SKIP LOCKED`, the initial attempt-count increment, the RETRY_PENDING backoff, and the RECOVERED transition — **except one spot** inside the supersession `NOT EXISTS`: `CROSS JOIN LATERAL telemetry.resolve_site_capture_bucket(ce.site_id, COALESCE(ts2.source_timestamp, r2m.received_at)) b2 … AND b2.bucket_start = ce.bucket_start` becomes a direct interval-containment test — `AND COALESCE(ts2.source_timestamp, r2m.received_at) >= ce.bucket_start AND COALESCE(ts2.source_timestamp, r2m.received_at) < ce.bucket_start + make_interval(secs => ce.capture_interval_seconds)`. `ce.bucket_start` / `ce.capture_interval_seconds` still come from `current_elements`' single `resolve_site_capture_bucket()` call, which stays the sole bucket-resolution authority and is unmodified; the `v_has_normalized` check keeps its once-per-candidate call. The trailing `COMMENT ON PROCEDURE` is re-issued with a `Migration 220:` paragraph but **without** the "Migration 203 (retained)" EXCEPTION paragraph — that paragraph is restored by migration 222 (already on `staging`), a forward correction that explicitly does not rewrite 220.
- **Why**: For a dense sub-8-second multi-device publisher (`E1_EM3`) the removed call ran *(competing rtdata elements in the `bucket_start..deadline` window)* × *(`current_elements` rows)* ≈ 2,900 invocations per candidate (~8 min), keeping Job 1077 pinned at its 10-minute `max_runtime` even after migration 218 deferred the uncommissioned publishers. `EXPLAIN` of the new form: no `Function Scan` on `resolve_site_capture_bucket`, supersession subquery cost 11458 → 210, the migration-204 bounded `received_at` index scan preserved.
- **Equivalence**: Under `WALL_CLOCK` alignment — the only deployed `alignment_mode`, asserted as a hard precondition by the regression tests — `resolve_site_capture_bucket(site, ts)` is the `capture_interval_seconds`-aligned floor of `ts`, so two timestamps share a `bucket_start` iff they fall in the same `[k·interval, (k+1)·interval)`; `capture_interval_seconds` cannot change inside the ~16-minute competing window. Read-only staging equivalence check (2026-08-29): candidate `raw_message_id` 2672476 + 3 more, all 364 competing `E1_EM3` elements across `bucket_start..deadline` (340 in the 900 s tail), **0 disagreements** with the bucket function; per-candidate supersede boolean matched for both superseded and not-superseded cases.
- **Explicitly NOT changed**: `telemetry.resolve_site_capture_bucket()` itself; `telemetry.run_failed_message_recovery_job()` and Job 1077 schedule / `max_runtime` / `config {limit:1000}`; `raw_message_failures` schema, the migration-218 `resolution_status` constraint, the `raw_message_failures_recovery_due_idx` predicate; retention/compression; any `replay_attempt_count` / `resolution_status` data; migrations 217 / 219; Grafana; application code.
- **Tests**: New `scripts/test/assert_recovery_supersession_dense_publisher.sh` / `.sql` (T1 last non-superseded packet in a dense bucket recovers — `capture_bucket_samples` + `normalized_points` — and the bare `CALL` over the dense population completes in < 5 s; T2 an earlier superseded same-bucket packet resolves to RECOVERED by supersession once its bucket is represented; T3 a same-bucket late arrival deep in the 900 s receipt tail is still a superseder; T4 a tail packet whose `source_timestamp` is in the next bucket is NOT a superseder; T5 structural — interval predicate present, per-element `b2` fan-out gone, exactly 2 `resolve_site_capture_bucket` call sites; T6 all `config.telemetry_capture_policies` rows are `WALL_CLOCK`), registered in `scripts/test/run_integration_environment.sh` after the late-arrival-bound test. `scripts/test/assert_recovery_supersession_late_arrival_bound.sql` gains TEST F asserting the same structural + `WALL_CLOCK` precondition. The dense-publisher fixture's raw-message loop uses a `v_rawid bigint` local (`telemetry.raw_messages.id` is `bigint`, not `uuid`) — carried with the migration-220 test set.
- **Evidence/source**: Original migration-220 work `5c44516` (`postgres/migrations/220_*.sql` sha256 `ab586cf0a06a45d767d8f2ebe85594188c770f665e6772522840151d45e6ba15`). Applied to the staging database out of band 2026-08-29 22:16 IST with its ledger identity/checksum preserved; this promotion brings the file, manifest row, and tests into the repository / `staging` branch history **without changing that applied identity**, so the normal CI/CD staging deploy is expected to record migration 220 as **SKIP** (DDL already live, ledger checksum matches). Local disposable-database integration suite green from a clean DB through migration 220.
- **Verified live**: **Staging — DDL already applied out of band 2026-08-29; repository/branch alignment via this promotion.** **Production — not touched**; Job 1000 remains paused; migration 221 is deliberately not part of this promotion.
- **Related**: `postgres/migrations/220_recovery_supersession_interval_predicate.sql`, `postgres/restructure_manifest.csv`, `scripts/test/assert_recovery_supersession_dense_publisher.sh` / `.sql`, `scripts/test/assert_recovery_supersession_late_arrival_bound.sql`, `scripts/test/run_integration_environment.sh`; `postgres/migrations/218_recovery_onboarding_aware_deferral.sql`, `postgres/migrations/206_*` / `204_*` / `203_*` / `202_*`, `postgres/migrations/222_recover_failed_raw_messages_comment_restore.sql` (EXCEPTION-paragraph restore).
- **Outcome**: Implemented and locally validated; staging promotion via normal CI/CD (expected migration-220 SKIP); production promotion awaits separate approval.
