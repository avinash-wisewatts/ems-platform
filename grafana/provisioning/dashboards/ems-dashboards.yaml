# Dashboard provisioning is tenant-managed by the EMS admin portal.
#
# The admin portal creates dashboards in the Grafana organization mapped in:
#   metadata.grafana_organization_map
#
# Do not add fixed orgId values here. Grafana organizations are created and
# reconciled dynamically and their numeric IDs differ between environments.

apiVersion: 1

providers: []
