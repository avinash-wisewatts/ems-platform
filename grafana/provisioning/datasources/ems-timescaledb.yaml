# Datasource provisioning is tenant-managed by the EMS admin portal.
#
# The admin portal provisions the EMS TimescaleDB datasource separately in
# each Grafana organization mapped in metadata.grafana_organization_map.
#
# Do not add fixed orgId values here. Fixed organization IDs become orphaned
# when tenants are removed or when the platform is installed elsewhere.

apiVersion: 1

deleteDatasources: []
datasources: []
